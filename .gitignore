node_modules/
dist/
# Backend runtime data — local secrets vault, configs, audit log. Never commit.
server/.data/
*.log

.env
.env.local

# Legacy build archives — kept on disk, never in git
*.zip

# Tenancy spike scratch output
server/spike/out/

# Top Gun test harness — credentials and run artifacts, never in git
tests/topgun/.env.topgun
tests/topgun/.artifacts/
