# UC-22 — Internal System Sync
status: active
catalog_home: skill
trigger: Manual
connectors: [FamiliOS:active, Gmail:optional-enrichment]
tool_ids: [homeops.write_memory, homeops.create_artifact, gmail.search (optional)]

---
**ACTIVE — built and locally verified (core).** Backing skill `skl_uc22_internal_system_sync` (server/seed.mjs).

## Intent (verbatim prompt)
"Take the audio transcription from our family meeting and parse it directly into our internal memory logs, then analyze our recent notification history to output a rapid dashboard digest of today's incoming alerts."

## Tool / step contract
| # | tool_id | action | approval | connector | active? | input keys |
|---|---------|--------|----------|-----------|---------|------------|
| 1 | homeops.write_memory | parse the meeting transcript into memory | false | FamiliOS | true | text, scope, type |
| 2 | homeops.create_artifact | compose today's alerts digest artifact | false | FamiliOS | true | title, body, kind |
| — | gmail.search (optional) | enrich the digest with live email alerts (`newer_than:1d`) | false | Gmail | true* | query |

\*Optional live-enrichment step, listed in the skill's `optional_tools`. Not in the executed core chain — it runs only when Google is connected.

## Correction — the digest is COMPOSED (ISS-002, EV-020)
There is **no built-in "recent inbox digest" tool** in FamiliOS (the internal registry has only the `homeops.*` tools). The facts handoff (ART-001) listed one; it does not exist. The digest is therefore COMPOSED from `homeops.write_memory` + `homeops.create_artifact` (and, when Google is connected, enriched by `gmail.search`). Every step cites a real tool_id; no non-existent internal tool is referenced.

## Acceptance criteria (observable, testable)
- AC1: A memory row is created from the meeting transcript (scope `family`).
- AC2: A `digest` artifact titled "Today's Alerts Digest" is created, composed from the transcript/activity.
- AC3: Every step's `tool_id` is a real `homeops.*` catalog id (no fabricated "inbox digest" tool).
- AC4: The run completes with both core steps `succeeded`.

## Test procedure
- Active path (verified now): `POST /api/runs/start {skillId: "skl_uc22_internal_system_sync"}`, poll to `completed`, inspect the memory + digest artifact.
- Automated: `node --test server/test/usecase-skills.test.mjs` — "UC-22 Internal System Sync" asserts AC1–AC4.

## Not-verified / blocked
- The `gmail.search` live-enrichment step is CONTRACT-only here (no Google account connected in the hermetic tenant). Unblock: connect Gmail (gmail.read) and the digest incorporates live inbox alerts.
