# UC-14 — Morning Status Text
status: active-partial
catalog_home: workflowTemplate + skill + automation
trigger: Schedule (daily 06:30)
connectors: [Weather:active, Text Messaging:active, RSS/Feed:inactive]
tool_ids: [weather.current, homeops.notify_contact, rss.latest (optional/stub)]

---
**ACTIVE-PARTIAL — built; delivery path and step graph verified. Weather is CONTRACT (egress), RSS is STUBBED (connector not configured).** Backing skill `skl_uc14_morning_status_text`, runnable agent `agt_morning_status`, catalog workflow template `wf-morning-status-text`.

## Intent (verbatim prompt)
"Every morning at 6:30 AM, fetch the current local outdoor weather conditions alongside the newest articles published to our family meal-prep RSS feed, compile the summary, and send it as a text message directly to my phone."

## Tool / step contract
| # | tool_id | action | approval | connector | active? | input keys |
|---|---------|--------|----------|-----------|---------|------------|
| 1 | weather.current | fetch current local conditions | false | Weather | true | — |
| — | rss.latest (optional/stub) | newest meal-prep feed items | false | RSS/Feed | **false** | feedUrl |
| 2 | homeops.notify_contact | text the compiled status via the registry | false* | FamiliOS registry (Text) | true | methodId, subject, body |

\*Registry delivery: standing consent = verified + opted-in + per-agent allowlist (DEC-005). **Uses `homeops.notify_contact`, NOT `sms.send`** — the unattended 6:30 AM schedule needs a route that can deliver without a per-run approval race; `sms.send` is approval-gated and would expire unread.

## Corrections (ISS-003, DEC-005, HYP-002)
- Delivery is `homeops.notify_contact`, never `sms.send`, on the unattended path.
- The RSS step is OPTIONAL/STUBBED: the RSS/Feed connector is not configured, so the meal-prep-feed section is omitted with a note; the run degrades gracefully. It is listed in the skill's `optional_tools`, not the executed chain.

## Acceptance criteria (observable, testable)
- AC1: The skill's executed chain is `weather.current → homeops.notify_contact`; it contains NO `sms.send`.
- AC2: `weather.current` and `homeops.notify_contact` resolve in the live catalog.
- AC3 (delivery): run as `agt_morning_status` against a verified, opted-in, allowlisted text method, `notify_contact` passes every registry consent gate and either delivers or returns the honest transport outcome — never a false success and never a bypass of the verified/opted-in/allowlist gates.
- AC4: When the RSS/Feed connector is unconfigured, the RSS section is omitted with a note (no fabricated feed content).

## Test procedure
- Contract path (verified now): `node --test server/test/usecase-skills.test.mjs` — "UC-14 Morning Status Text" asserts AC1–AC2 (chain + no sms.send + catalog resolution). The delivery-gate behavior (AC3) is pinned by server/test/notify-contact-delivery.test.mjs (same `homeops.notify_contact` tool, exercised end-to-end through every gate).
- Active real-run path (needs a connected Weather + a text transport): verify a Phone/Text method, allowlist `agt_morning_status`, run the skill, and inspect the delivery audit for real weather in the text.

## Not-verified / blocked
- Live weather fetch (egress) and real text delivery are connector-dependent and not exercised in the hermetic tenant. RSS is blocked until the RSS/Feed connector is configured. Unblock: connect Weather + a text transport; configure RSS/Feed to enable the meal-prep section.
