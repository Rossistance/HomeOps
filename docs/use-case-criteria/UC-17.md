# UC-17 — Smart Recipe Extractor
status: active
catalog_home: skill
trigger: Manual
connectors: [Web Search & Reading:active]
tool_ids: [web.search, web.read, web.recipe]

---
**ACTIVE — built; step graph and tool contract locally verified.** Backing skill `skl_uc17_recipe_extractor` (server/seed.mjs).

## Intent (verbatim prompt)
"Search the web for 'quick kid-friendly weeknight dinner recipes', open and read the top-ranked result page, and cleanly isolate the ingredients and preparation steps while stripping out the ads."

## Tool / step contract
| # | tool_id | action | approval | connector | active? | input keys |
|---|---------|--------|----------|-----------|---------|------------|
| 1 | web.search | search for recipe pages | false | Web Search & Reading | true | query |
| 2 | web.read | read the top result page (URL threads from step 1) | false | Web Search & Reading | true | url |
| 3 | web.recipe | extract structured recipe, ads stripped | false | Web Search & Reading | true | url |

## Acceptance criteria (observable, testable)
- AC1: The skill's step chain is exactly `web.search → web.read → web.recipe`, all `Read`, none requiring approval.
- AC2: All three tool_ids resolve in the live tool catalog (no invented ids).
- AC3: On a real run against a recipe URL, `web.recipe` returns `{recipe:{ingredients[], instructions[], source}}` with ads stripped.
- AC4: When a page has no structured recipe, `web.recipe` returns an honest null (no fabricated recipe) — covered by server/test/web-tools.test.mjs ("recipeFromHtml returns null when a page has no structured recipe").

## Test procedure
- Contract path (verified now): `node --test server/test/usecase-skills.test.mjs` — "UC-17 Recipe Extractor" asserts AC1–AC2, plus the honest-null extraction is pinned by web-tools.test.mjs (AC4).
- Active real-run path (needs web egress): boot the local stack with outbound web access, `POST /api/runs/start {skillId:"skl_uc17_recipe_extractor"}`, and inspect the `web.recipe` result for AC3.

## Not-verified / blocked
- Live web egress is not available in the hermetic test tenant, so the end-to-end fetch (AC3) is not exercised in CI — it is verified by running the skill on the local stack with network access. The extraction logic itself is unit-covered (AC4).
