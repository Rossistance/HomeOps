# UC-12 — Smart Climate Night-Mode
status: active (device-runtime-unverified)
catalog_home: workflowTemplate + skill
trigger: Schedule (nightly 22:00)
connectors: [Google Home:active]
tool_ids: [smarthome.listDevices, smarthome.setThermostat]

---
**ACTIVE — built; command contract and approval gating locally verified. Runtime-device-unverified** (no physical Nest + `HOMEOPS_SDM_PROJECT_ID` unset). Backing skill `skl_uc12_climate_nightmode`, catalog workflow template `wf-smart-climate-night-mode`.

## Intent (verbatim prompt)
"Scan all connected smart hardware profiles across the house to ensure everything is online, then automatically adjust the central living room thermostat down to 68 degrees at 10:00 PM every night."

## Tool / step contract
| # | tool_id | action | approval | connector | active? | input keys |
|---|---------|--------|----------|-----------|---------|------------|
| 1 | smarthome.listDevices | confirm connected devices are online | false | Google Home | true | — |
| 2 | smarthome.setThermostat | set living-room thermostat to 20°C (68°F) | **true** | Google Home | true | deviceId, celsius |

## Acceptance criteria (observable, testable)
- AC1: The skill's step chain is exactly `smarthome.listDevices → smarthome.setThermostat`.
- AC2: `smarthome.setThermostat` resolves in the live catalog as `Write` with `requiresApproval: true`; the skill's step 2 has `approval_required: true`.
- AC3 (contract): the setThermostat step emits the correct SDM command shape (`sdm.devices.commands.ThermostatTemperatureSetpoint.SetHeat`, `heatCelsius: 20`) with approval.
- AC4 (fail-closed): with `HOMEOPS_SDM_PROJECT_ID` unset, the tool fails closed with the exact setup message rather than pretending to set a device.

## Test procedure
- Contract path (verified now): `node --test server/test/usecase-skills.test.mjs` — "UC-12 Smart Climate Night-Mode" asserts AC1–AC2 (step graph + approval gating + catalog resolution). Command-shape + fail-closed behavior (AC3/AC4) are covered by the smart-home provider tests (server/test/smart-home-providers.test.mjs).
- Active real-run path (needs a physical Nest + SDM project id): set `HOMEOPS_SDM_PROJECT_ID`, connect Google Home with a real thermostat, run the skill, approve the setpoint, and observe the device change.

## Not-verified / blocked
- **Runtime-device-unverified**: no physical thermostat is in the loop and `HOMEOPS_SDM_PROJECT_ID` is unset, so the actual device state change is NOT observed. Unblock: set `HOMEOPS_SDM_PROJECT_ID` and connect a Nest device (ISS-004).
