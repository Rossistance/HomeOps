# UC-01 — School Correspondence Organizer
status: active
catalog_home: workflowTemplate + skill
trigger: Schedule (weekday morning)
connectors: [Gmail:active]
tool_ids: [gmail.search, gmail.listLabels, gmail.modifyLabels]

---
**ACTIVE — built; step graph and approval contract locally verified.** Backing skill `skl_uc01_school_correspondence`, catalog workflow template `wf-school-correspondence`.

## Intent (verbatim prompt)
"Every weekday morning, look through my personal inbox for updates from the school district, see how my sorting tags are set up, and automatically sort those messages out of my main feed so I can look at them later."

## Tool / step contract
| # | tool_id | action | approval | connector | active? | input keys |
|---|---------|--------|----------|-----------|---------|------------|
| 1 | gmail.search | find school-district mail (`from:(school OR district OR pta OR daycare) newer_than:1d`) | false | Gmail | true | query, maxResults |
| 2 | gmail.listLabels | read the current label set | false | Gmail | true | — |
| 3 | gmail.modifyLabels | add "School", remove "INBOX" (label/move) | **true** | Gmail | true | messageIds, addLabels, removeLabels |

## Acceptance criteria (observable, testable)
- AC1: The skill's step chain is exactly `gmail.search → gmail.listLabels → gmail.modifyLabels`.
- AC2: `gmail.modifyLabels` resolves in the live catalog as `action: Write`, `risk: High`, `requiresApproval: true`; the skill's step 3 has `approval_required: true` — the label/move never auto-runs.
- AC3: On a real run with a connected Gmail account and ≥1 school message, the write returns `{modified ≥ 1, added:["School"], removed:["INBOX"]}` after approval; non-school mail is untouched.
- AC4: With the `gmail.modify` scope missing, the tool returns an honest typed error (reconnect prompt), not a silent success.

## Test procedure
- Contract path (verified now): `node --test server/test/usecase-skills.test.mjs` — "UC-01 School Correspondence" asserts AC1–AC2 (step graph + approval gating + catalog resolution).
- Active real-run path (needs a connected Gmail with gmail.modify): connect a **test** Gmail account, `POST /api/runs/start {skillId:"skl_uc01_school_correspondence"}`, approve the parked label/move step, and inspect the message labels for AC3.

## Not-verified / blocked
- Live Gmail mutation (AC3/AC4) is NOT exercised here: no Google account is connected in the hermetic tenant, and this build must never mutate a real inbox. Unblock: connect a test Gmail account with the `gmail.modify` permission. The approval gate is proven; only the live write remains connector-dependent.
