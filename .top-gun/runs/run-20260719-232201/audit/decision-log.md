# Decision and Rationale Log — run-20260719-232201

| DEC-ID | Decision | Alternatives | Why preferred | Constraints/evidence | Preserved strengths | Trade-offs | Deferred | Reversal condition |
|---|---|---|---|---|---|---|---|---|
| DEC-01 | ISS-001 fix is SERVER-first (respond handler reassigns + dedupes), with thin client rendering updates | (a) client-side: after accept, client PATCHes the task itself; (b) server-side transactional reassign | (b): repro proves the gap is server truth; client-side would race, duplicate authority, and leave web/mobile inconsistent; server already owns notify+audit on respond | EV-NET-01, EV-CODE-03; task PATCH primitive exists | Approval/notification pattern; "asking is role-free" policy | Server change touches web too (shared backend) — needs web render check | Ownership *history* (who helped when) beyond audit log | If product decides "helping" ≠ "owning" (assist model), reassignment becomes a task-share flag instead |
| DEC-02 | TF-012 classified as P2 feedback/categorization gap, not P1 data loss | keep P1 pending device forensics | Two-attempt + round-trip repro shows durable persistence and honest caps; remaining failure modes are perception-layer and fully addressable | EV-NET-01, EV-CODE-05, EV-SS-13 (file present) | Server file store design (multi-page, caps, blobs) | Small residual risk her exact failure was device-specific (labeled HYP-06) | — | A reproducible lost-upload trace would restore P1 |
| DEC-03 | Event editor work = ONE coherent pass (end-date + all-day + notes: ISS-004/005/006) rather than three micro-fixes | ship notes alone first (unblocks ISS-003) | The three share the same form section, model surface, and calendar render; one pass avoids re-touching stamp()/validation thrice; server needs only the allDay concept | EV-CODE-01/07, EV-NET-02 | Existing form patterns (@expo/ui pickers, Well/SectionHeader design system) | Slightly bigger slice before ISS-003 lands full value | Recurring events (no field evidence) | If budget forces cuts, notes-only is the minimal ISS-003 unblocker |
| DEC-04 | ISS-003 description = compose notes + "Bring:" list into Google description at the payload builder (server), keeping pull-merge lossless | mobile client composes description before push | Server builder is the single choke point used by push route, auto-sync, sweep, and meals — client-side would miss the auto-sync path | EV-CODE-04 (four call sites listed in comment) | Two-way sync architecture | Description round-trip: pull must not re-import the composed "Bring:" block into notes verbatim — needs a delimiter convention | Structured Google extended-properties mapping | If Google extendedProperties adopted later, move whatToBring there |
| DEC-05 | ISS-007 autocomplete via expo-location `geocodeAsync` (on-device/OS geocoder), debounced, no new API key; Directions via platform maps URL | Google Places API (better ranking, costs + key + privacy), Mapbox | Zero-credential, zero-cost, offline-tolerant, and honest — matches the app's no-silent-external-calls posture; quality sufficient for named places + addresses | EV-CODE-09 (expo-location already shipped) | Permission posture (when-in-use only, already declared) | Rankings weaker than Places for POI names | Places provider behind a connector if quality proves insufficient | Tester feedback that suggestions miss their real venues |
| DEC-06 | ISS-008: keep the approval gate; merge surfaces as "Save changes" + inline "also update Google" consent (remembered), not silent auto-push | (a) one button that always pushes silently; (b) status quo | (a) violates PI-07 (tool access ≠ permission, external writes gated); status quo is the confirmed confusion; the middle path preserves consent with one primary action | EV-CODE-01, PI-07 | Approval-gated external writes — a core trust strength worth preserving explicitly | One extra inline consent on first push per event | Full auto-sync default flip (calendarAutoSync) | If household enables calendarAutoSync, UI collapses to one button naturally |
| DEC-07 | Runtime evidence routing: local backend API = authority verification; native rendering claims stay code-traced + field-screenshot-corroborated, labeled native-verification-blocked; no `npx expo` web attempt this run | try expo web target for pseudo-native evidence | The reported defects' truth lives server-side (proven) or in form logic (code-evident); an expo web render would produce WEB evidence that must not be claimed as native anyway (runtime-drivers hard constraint) and burns budget | runtime-drivers.md precedence; facts handoff driver probes | — | No current-build visual confirmation of native UI | expo web smoke could sanity-check form logic if implementation phase wants it | Drivers unblocked → run the blocked verifications on-device |
| DEC-08 | Google push probe halted after one 401-rejected attempt; TF-003 stays contract-level | retry probing push with a fresh event | Mission forbids live Google calls; the route reached Google because the resident local household holds a stale account — one rejected call is a disclosed incident, not a lane | EV-NET-02 P3; brief stop conditions | — | needsApproval path of the push route unobserved at runtime (code-traced only) | — | Explicit user authorization for a sandboxed Google test account |
| DEC-09 | Recommended bundle = TF-WP-A analog (help-loop integrity) + TF-WP-B analog (upload feedback) + event-editor pass + Google-payload fix, before location/task-edit features | severity-strict order only (bugs then each feature singly) | Bundles the two P1/P2 trust repairs with the editor pass that unblocks sync fidelity — one coherent "the app tells the truth and the calendar works" release matching HANDOFF's candidate sequence and the field pain | issue register dependencies | — | Location intelligence + task editing wait one release | — | User selects differently at the gate (pre-selection recorded: recommended bundle) |
| DEC-10 | Native iOS visual confirmation POSTPONED by user (2026-07-20): removed as a requirement/blocker for release and for future /top-gun missions. Appium/BrowserStack/Appetize unblock efforts closed as an ongoing task; ship on code-trace + local-API + field-screenshot evidence, with field-tester feedback on TestFlight builds as the native visual lane | keep native lane as standing blocker until creds supplied | User decision, delivered post-mission: unblocking device-cloud creds is not worth delaying release; TestFlight field testers provide real-device coverage | User instruction 2026-07-20 (commit/deploy/TestFlight build 20 order); no BROWSERSTACK_*/APPETIZE_* creds ever materialized | Honest evidence labeling stays — native claims remain marked code-traced, never upgraded to verified | Native render regressions surface via field testers, not pre-release | element-map.md + WP-008 testID blueprint stay ready if the lane is ever revived | User re-prioritizes native automation and supplies device-cloud credentials |
