# Decision and Rationale Log

DEC-10 (prior run run-20260720-073025 — native iOS visual verification lane closed) is cited as external lineage and is NOT re-opened here.

| DEC-ID | Decision | Alternatives | Why preferred | Constraints/evidence | Preserved strengths | Trade-offs | Deferred | Reversal condition |
|---|---|---|---|---|---|---|---|---|
| DEC-001 | Every ACTIVE use-case ships as TWO artifacts: a declarative catalog entry (agent/workflow/playbook) AND a backing executable Skill with real `tool_id` steps. | (a) catalog entry only; (b) skill only. | (a) is not runnable/verifiable (catalog fields are display strings); (b) loses in-product discoverability + the template UX the user asked for. Both layers together satisfy "build as templates" AND "verify end-to-end". | EV-001,EV-002,EV-003,EV-006,EV-011; PI-02 | The existing 12/20/14 catalog + skill/function architecture is sound and reused as-is. | More surface per item (two files + catalogIds wiring). | — | If a future runtime lets a WorkflowTemplate carry executable steps directly, collapse to one artifact. |
| DEC-002 | Catalog-home rule: workflowTemplate for a single triggered flow; agentTemplate for ongoing domain ownership (multi-trigger, monitoring); skill for FamiliOS-native multi-step actions; playbook for guidance-only. | One-size (everything an agentTemplate; or everything a workflow). | Matches each item's trigger/tool shape and the existing catalog's own conventions (EV-001/002). UC-01 and UC-21 justify agent ownership; UC-17/18/19/20/22 are native skills; most others are triggered workflows. | EV-001,EV-002,EV-005 | Mirrors how the shipped catalog already splits agents vs workflows vs playbooks. | Some items are defensibly either agent or workflow; documented per item in the queue. | — | Per-item home can be revisited if the user prefers a uniform surface. |
| DEC-003 | One criteria-doc format under `docs/use-case-criteria/`, one file per UC (UC-01.md…UC-22.md) + a `_TEMPLATE.md` + `README.md`; acceptance is connector-decoupled (contract/mock-level for inactive). | (a) criteria inside each skill's test_cases only; (b) a single monolithic criteria doc. | The user explicitly wants durable, per-item, selectively-testable criteria saved in the project WITHOUT requiring inactive connectors. Per-file + committed + mock-level meets all three. | ART-001 §Mission; PI-06 | Reuses the skill `test_cases[]` mechanism for the active items as the executable half. | Some duplication between a UC doc and its skill test_cases (kept in sync by the doc format). | — | If the app grows a native criteria store, migrate the docs into it. |
| DEC-004 | Confirm the 9 active / 13 inactive split, with four corrections (UC-12 device-unverified, UC-14 RSS-partial, UC-20 attach mismatch, UC-22 digest-compose). | Accept ART-001 verbatim; or reclassify UC-04/07/11 as active via native variants. | The 9/13 split is correct against the real catalog; the four corrections are code-grounded (EV-018/019/020/021). Reclassifying UC-04/07/11 would violate the source's declared connectors — kept as opportunities instead (ISS-007). | EV-007..021 | Honest labeling; no inactive item claimed verifiable. | UC-12/14/20/22 carry caveats that complicate their acceptance criteria. | — | New connector activation or a new internal tool flips a specific item's class. |
| DEC-005 | Unattended/scheduled delivery uses `homeops.notify_contact` (registry allowlist), not approval-gated `gmail.send`/`sms.send`. | Use `sms.send`/`gmail.send` as the source literally lists. | Approval-gated sends expire unattended (the already-fixed 7 AM briefing failure). notify_contact is the deliberately-designed scheduled path with fail-closed consent gates. | EV-021; PI-04 | Reuses the shipped WP-005 delivery design. | The source's literal "Text Messaging · Send" is honored as the channel, but delivered via the registry. | — | If a per-run approval UX for scheduled sends is added, revisit. |
| DEC-006 | Live-record seeding on production is UI/user-side; local-stack `materializeBuild` (TG- data, cleanup) is the verification path. | Attempt scripted prod seeding. | Scripted prod mutation is classifier-blocked (EV-017); attempting it would fail and risks touching real family data. Local materialization proves the contract safely. | EV-013,EV-017; ART-001 §Authority | Keeps production read-only and the real tenant safe. | The "live seed" half of BOTH is delivered as code + a user-run path, not scripted on prod. | Live prod seeding deferred to the user (UI or run). | If a safe server-side seed endpoint is authorized, script it. |
| DEC-007 | Personas + journeys are DESIGN-TIME, grounded in the real seed roster and tool contracts, labeled synthetic. | Skip personas (build-from-spec); or run live journeys. | Nothing is built yet and product source is read-only pre-selection, so live journeys are impossible; design-time journeys over the real contracts + seed roster are the honest maximum. Mandate requires exactly five personas. | EV-006; mandate §739 | Uses the real seeded family (Alex/Morgan/Lily/Noah/Elaine/Sam). | Journeys are contract/spec-level, not executed runs — disclosed. | Live journeys deferred to implementation phase. | Once built, re-run journeys as real executions. |
