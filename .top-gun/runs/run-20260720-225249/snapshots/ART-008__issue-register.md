# Master Issue Register

Deduplicated, root-caused risks to the 22-use-case build plan. These are build-readiness/design issues (not product runtime defects — baseline is green, EV-014/015/016). Ordered by severity then dependency. Severity here = impact on delivering a truthful, verifiable build.

| ISS-ID | Title | Severity | Confidence | Personas | FEAT/JRN IDs | Visual symptom | Technical symptom | Root cause | Evidence IDs | Impact | Recommended resolution | Files/services | Dependencies | Acceptance criteria | Validation | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ISS-001 | UC-20 "attach to task card" has no supporting tool | P2 | confirmed | JRN-5,JRN-4 | FEAT-020 | User expects a doc reference on the chore card; none appears | `attach_note_or_file_reference` operates on EVENTS (`getEvent`/`eventId`), not tasks; `create_task` has only a `notes` field | Source use-case assumes a task-attachment capability the internal registry does not expose | EV-019 | Building UC-20 literally would fabricate an unsupported step | Map the "attach" step to the task's `notes` (embed the file reference/URL there), OR create a companion event to hold the attachment; document the limitation in the criteria doc. Do NOT invent a task-attachment tool. | server/internal-functions.mjs; UC-20 skill | — | UC-20 skill runs end-to-end; the doc reference is persisted and retrievable on the task (via notes) with no fabricated success | Local `runSkill`, inspect task record | open |
| ISS-002 | UC-22 "recent inbox digest" internal function does not exist | P2 | confirmed | JRN-1 | FEAT-022 | User expects a one-tap "today's alerts" digest | ART-001 §Capability lists a "recent inbox digest (internal)" tool; the internal registry has no such tool (only the 13 homeops.*) | Facts handoff overstated the internal catalog | EV-020 | Building UC-22 as a single internal call would cite a non-existent tool_id | Compose the digest: `homeops.write_memory` (meeting notes) + `gmail.search` (Google active) and/or a reasoning step over server activity → `homeops.create_artifact` (the digest). Correct ART-001. | server/internal-functions.mjs; UC-22 skill | Google connected | UC-22 skill produces a real digest artifact from real inbox/activity data; every step cites a real tool_id | Local `runSkill`, inspect artifact | open (correction journaled, event #8) |
| ISS-003 | UC-14 unattended 6:30 AM text via `sms.send` would expire on approval | P2 | confirmed | JRN-1,JRN-3,JRN-4 | FEAT-014 | 6:30 AM text never arrives | `sms.send` is approval-gated (High); an unattended schedule has no one to approve, so the send expires — the exact 7 AM-briefing failure mode already fixed via WP-005 | Source specifies "Text Messaging · Send"; the scheduled path must use the registry, not a present-user send | EV-021 | Literal build delivers nothing unattended | Route scheduled delivery through `homeops.notify_contact` (verified + opted-in + per-agent allowlist); reserve `sms.send` for present-user runs. Verify with a `TG-` verified test method. | server/internal-functions.mjs; UC-14 skill/automation | A verified TG- contact method + agent allowlist | A scheduled UC-14 run delivers the text via notify_contact to a verified TG- method; honest refusal if unverified | Local scheduled-run harness | open |
| ISS-004 | UC-12 smart-home tools fail closed without SDM project id + physical device | P3 | confirmed | JRN-2 | FEAT-012 | Thermostat never changes | `smarthome.listDevices`/`setThermostat` require `HOMEOPS_SDM_PROJECT_ID`; both throw a fail-closed setup error until set; no physical Nest confirmed in-loop | Google smarthome scope is granted but the Device Access project + device are additional prerequisites | EV-018 | UC-12 is build-ready at the tool-call contract level but cannot be device-verified now | Build + verify the tool-call contract (correct ids, inputs, approval, fail-closed message) against a mocked device; label runtime-device-unverified; name the unblock (set SDM project id + connect a Nest). | server/providers.mjs; UC-12 skill | HOMEOPS_SDM_PROJECT_ID; physical Nest | Contract test passes; skill emits the correct executeCommand shape; documented device-unverified | Contract test / mocked device | open |
| ISS-005 | 13 use-cases depend on inactive connectors (spec+criteria only) | P2 | confirmed | JRN-1,JRN-2,JRN-3 | FEAT-002..011,013,015,016 | Feature cannot run | Required tool ids exist in code but their connector reports not connected / not configured / runtime unavailable | Connector breadth in the source doc exceeds live provisioning (MS365/Slack/Dropbox/Notion/Todoist/TickTick/Alexa/RSS/HTTP/Browser) | EV-007,EV-008,EV-017 | 13 items cannot be built+verified now; must be honestly labeled NOT implemented | Deliver each as a spec + connector-decoupled criteria doc (WP-010); implement only when its connector is wired. Never claim verified. | catalog + docs/use-case-criteria | Connector activation by the user | Each inactive item has a spec + criteria doc, clearly labeled not-implemented, with the exact connector to activate | Doc review | open (by design) |
| ISS-006 | "Live seed records" half of BOTH is scripted-mutation-blocked on prod | P3 | confirmed | JRN-1 | PI-08,FEAT-001..022 | Live records don't appear on the account via script | Classifier denies scripted POST/PATCH/DELETE to production; `materializeBuild` is reachable locally, not via scripted prod mutation | Production safety guardrail | EV-013,EV-017 | The active templates can be seeded live only via the app UI or a user-run seed script | Ship code templates + provide a seed script/spec the user runs (or UI steps); verify materialization on the LOCAL stack with TG- data. Flag as a delivery constraint, not a failure. | server/index.mjs (/api/assistant/build); seed script | User-side UI or run | Local materialization verified; a documented UI/seed path exists for prod | Local materialize run | open (delivery constraint) |
| ISS-007 | Three inactive items have a trivially-active native alternative | P3 | strongly-inferred | JRN-3,JRN-2 | FEAT-004,FEAT-007,FEAT-011 | User could get value now but source specifies an inactive connector | UC-04 (grandparent digest) → gmail.send/notify_contact; UC-07 (Drive→OneDrive backup) → Google Drive is active, only the OneDrive sink is inactive; UC-11 (Todoist list) → native tasks list | Source declared the connector; a native variant exists | EV-007,EV-009 | Opportunity to deliver value now without waiting on a connector | Offer active native variants as OPTIONAL companion templates (opportunity register); keep the source items classified inactive to honor the declared connector. | catalog | product decision | Native-variant templates offered as opt-in; source items remain honestly inactive | Doc/opportunity review | open (opportunity) |
| ISS-008 | Runtime/engine drift: node 25.8.2 vs engines `<25` | P3 | confirmed | — | EV-022 | none | `package.json` engines `>=22.13 <25`; mandated runtime 25.8.2 | Engines range not updated for the newer runtime | EV-022 | Advisory only; tests + both tscs pass under 25.8.2 | Optionally widen `engines.node` to include 25.x when the implementation lead touches package.json; not blocking. | package.json | — | engines range reflects the supported runtime; CI green | tsc + npm test under 25.8.2 | open (advisory) |
