# Hypothesis Queue

Open questions the implementation lead must resolve during the build; each has an exact discriminating check. All are verifiable on the LOCAL stack (TG- data) — none require touching production or a real family contact.

| HYP-ID | Question | Owner | Confidence | Needed evidence | Next discriminating check | Status | Resolution |
|---|---|---|---|---|---|---|---|
| HYP-001 | Does the UC-01 chain (gmail.search → listLabels → modifyLabels) auto-create a "School" label and archive from INBOX correctly on a real Gmail? | implementation-lead | hypothesis | A real run against a test Google account or a contract fixture | Local `runSkill` with a test Gmail account (or a mocked provider api()) exercising `modifyLabels` addLabels="School" removeLabels="INBOX" | open | — |
| HYP-002 | Will a 6:30 AM unattended UC-14 run actually deliver the text via `notify_contact` given a verified TG- method + agent allowlist? | implementation-lead | hypothesis | A scheduled/triggered local run + delivery audit row | Create a TG- verified contact method + allowlist the agent; fire the schedule locally; inspect `deliverNotification` audit | open | — |
| HYP-003 | Will `smarthome.setThermostat` produce the correct SDM executeCommand and succeed once `HOMEOPS_SDM_PROJECT_ID` + a Nest are present? | implementation-lead | hypothesis | Contract-shape assertion now; live device later | Assert the request body shape (`sdm.devices.commands.ThermostatTemperatureSetpoint.SetHeat`, heatCelsius) via a mocked api(); label live-device pending | open (blocked on device) | — |
| HYP-004 | Can `materializeBuild` stand up a skill+agent+automation from a UC build spec cleanly on the LOCAL stack (the "live seed" contract)? | implementation-lead | hypothesis | Local POST /api/assistant/build with a TG- spec → created ids | Boot local stack; POST a minimal UC-18 build spec (Adult Admin session); confirm skill(draft)+agent created; clean up TG- records | open | — |
| HYP-005 | For UC-20, is embedding the document reference in `task.notes` an acceptable stand-in for the missing task-attachment tool? | implementation-lead | hypothesis | Product judgment + a runnable skill | Build UC-20 with the note-embed approach; confirm the reference is retrievable on the task; record in the criteria doc | open | — |
| HYP-006 | Does the household prefer the active native variants (UC-04/07/11) as companion templates, or strictly the source's inactive versions? | user / implementation-lead | proposed | A product decision | Surface ISS-007 in the numbered menu as an optional add-on bundle | open | Deferred to selection gate |
