# Master Issue Register — run-20260720-073025

One row per root cause, deduplicated; ordered by severity then dependency.

| ISS-ID | Title | Severity | Confidence | Personas | FEAT/JRN IDs | Visual symptom | Technical symptom | Root cause | Evidence IDs | Impact | Recommended resolution | Files/services | Dependencies | Acceptance criteria | Validation | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ISS-001 | Chat-built automations never run the built skill: trigger target loses skill/goal, scheduled fires degrade to a read-only "status pass" (or a zero-step run) that reports `completed` | P0 | observed | Alex, Morgan | FEAT-005/006/007, JRN-1/2 | Automations screen shows the run "completed"; chat said "Done — I set up …"; no briefing ever arrives | Trigger record `target:{agentId, skillId:null, goal:null}`; fired run = permitted∩available non-approval READ tools (1 garbage web.search, or 0 steps), status `completed` | `normalizeBuild` strips everything but {name,type,intervalMs,runAt} (structurally cannot carry a target) and `materializeBuild` falls back to agent-only target; `fireTriggerInner`→`runAgent` with no goal/skillId → `buildReadonlyPlan` [observed, mechanism fully traced] | EV-001, EV-002, EV-003, EV-004, EV-011, EV-015 | EVERY chat-built scheduled agent is inert while reporting success — the user's core complaint; total trust collapse in the flagship "build helpers from chat" promise | Wire the created skill into the trigger target (`skillId` + goal fallback from agent instructions); make `fireTriggerInner` prefer `runSkill`; refuse to create an automation whose target resolves to nothing runnable | server/index.mjs (materializeBuild), server/planner.mjs (normalizeBuild), server/triggers.mjs | none | Given the repro-A chat request, the created trigger's target carries the created skillId; firing it produces a run whose steps are the SKILL's steps (incl. the gmail.send step parking for approval); a build that would produce an unrunnable target returns an honest error in chat | Re-run audit/evidence/repro-false-success.mjs repro A; assert target.skillId != null and run steps == skill steps | open |
| ISS-002 | Vacuous success: "send/notify" steps encoded as toolId:null reasoning steps always `succeed`; chat then says "Done — finished (n/n steps)" and even "That worked. Want me to save…" with zero external effect | P0 | observed | Alex, Morgan | FEAT-003/009/014, JRN-1 | Chat celebrates a finished briefing email; inbox stays empty | Run `completed` 3/3 where step 3 "Send email to wrhixon@gmail.com" is toolId:null; run_result counts it done | ASSISTANT_SYS explicitly instructs toolId:null for "reasoning/notify/summarize" steps; engine reasoning branch unconditionally marks `succeeded` (even provider-less); `runOutcomeText` counts steps without distinguishing effects [observed] | EV-005, EV-008, EV-012, EV-023 | Every task type in chat can "complete" without doing anything external — matches the user's "every single type of task" report | Planner: forbid null-tool steps whose title/detail claims an external effect (validate in normalizePlan); engine: mark reasoning steps `succeeded (reasoning-only)` and exclude from effect claims; runOutcomeText: state what was actually delivered vs composed | server/planner.mjs, server/engine.mjs, server/assistant-runs.mjs | none | The repro-B plan either maps the send to a real tool (parking honestly) or the run_result explicitly says "composed but NOT sent — no send tool was available"; no "That worked" offer for effect-free runs | Re-run repro B; assert run_result text contains an explicit not-sent disclosure | open |
| ISS-003 | "Daily at 7 AM" is unexpressible: chat-built recurring triggers anchor to creation time (nextRunAt = now+intervalMs); no time-of-day or timezone anchor exists in the build spec | P1 | observed | Alex | FEAT-006, JRN-1 | Automation card shows next run at the creation hour, not 7 AM | `nextRunAt_minus_createdAt = 24.00h` for the "7 AM" request | Build JSON shape offers only intervalMs (runAt documented for "schedule" type only); createTrigger has no anchor/tz concept [observed] | EV-002, EV-003, EV-011, EV-023 | Even after ISS-001 is fixed, the briefing would arrive at whatever hour the user happened to create it | Add runAt (first occurrence) to the build spec for recurring + prompt guidance to compute it from the user's stated time + household tz; optionally an `anchor` field ("07:00" local) recomputed per fire to prevent drift | server/planner.mjs (ASSISTANT_SYS + normalizeBuild), server/triggers.mjs, server/index.mjs | ISS-001 (ordering only) | For "every day at 7 AM", trigger.nextRunAt lands on the next local 07:00 (±1 min) and successive fires stay at 07:00 | Unit test on createTrigger/updateTrigger + repro A assertion on nextRunAt hour | open |
| ISS-004 | Parked and expired runs vanish silently: waiting_for_approval / waiting_for_connector runs never report to the chat; approval expiry (30-min TTL) skips the run-finished hooks; mobile watchers give up quietly (~3–5 min) | P1 | observed (expiry end-state: code-traced) | Alex, Morgan | FEAT-010/014/019, JRN-1 | Chat's last words: "On it — sending the briefing…" — then nothing, forever | Run parked `waiting_for_approval`; no later message; expireStaleRuns → `expired` without fireRunFinished; client polls stop at 120×1.5s / 5-min cap | Hooks fire only on completed/failed; expiry path omits fireRunFinished; clients have no terminal-state message for parked runs [observed + code] | EV-007, EV-008, EV-013, EV-021, EV-022 | The honest fail-closed layers (approvals, connectors) are INVISIBLE — the user experiences them as "said it was doing it, nothing happened" | Append run_result-style messages on park ("waiting for your approval — expires in 30 min", with deep link) and on expiry/stall; call fireRunFinished for expired runs; surface waiting state in chat clients past the polling caps | server/engine.mjs, server/assistant-runs.mjs, apps/mobile/(ask)+run-context, src/screens/Assistant.tsx | none | Repro B2 conversation contains a "waiting for approval" message within 5 s of parking and an "expired without a decision" message after TTL; mobile thread shows the same after reopen | Harness test with shortened TTL; re-run repro B2 | open |
| ISS-005 | Silent plan clamping: orchestrator drops policy-disallowed tool steps before the run starts; the run then completes and reports success with the send step simply missing (droppedSteps recorded only in routing) | P1 | observed | Alex | FEAT-020, JRN-1 | Run looks complete; nobody is told a step was removed | Goal plan had gmail.send; started run has 2 steps; status `completed` | runAgent filters steps by isToolStepAllowed and proceeds silently (engine's own re-validation would have failed loudly — the pre-clamp hides it) [observed] | EV-004, EV-014, EV-024 (hist: policy_block on the engine path) | An agent whose policy misses one tool quietly does PART of the job and claims all of it | Replace silent drop with visible skipped steps (status `skipped: not permitted`) or fail the run start with an honest message naming the missing permission | server/orchestrator.mjs, server/engine.mjs | none | A goal plan containing a disallowed tool yields either a run showing that step as skipped-with-reason or an error naming the tool; chat summary mentions it | Re-run repro C; assert the send step is visible with a policy reason | open |
| ISS-006 | No engine-reachable path to real scheduled email: gmail.send needs the acting person's own Google account + per-run approval (30-min TTL vs 7 AM cadence); registry delivery (deliverNotification) is HTTP-only, not a tool; send_notification_draft only drafts | P1 | observed | Alex, Morgan, Elaine | FEAT-011/012/013, JRN-1/5 | — | Catalog's only "email" options: gmail.send (parks; scheduler actor has no accounts) or a draft tool; /api/notify unreachable from runs | Delivery architecture gap: the safe registry path was never exposed as an executable function [observed] | EV-009, EV-010, EV-013, EV-017 | Even a perfectly built + scheduled agent cannot deliver unattended email; users cannot succeed no matter what they type | Add an internal engine tool (e.g. homeops.notify_contact) wrapping deliverNotification (verified + opt-in + per-agent allowlist = the standing-consent model), so scheduled sends ride the registry gates instead of per-run approvals; keep gmail.send for ad-hoc personal sends | server/internal-functions.mjs, server/notify.mjs, server/planner.mjs (catalog) | ISS-001 (to be reachable from chat builds) | A scheduled skill step `homeops.notify_contact {methodId}` delivers via the owner's connected Google (or honestly returns needsSetup) with NO per-run approval when the method allowlists the agent | Harness test with mocked Gmail fetch (family-safety.test.mjs pattern); manual live test only with explicit user authorization | open |
| ISS-007 | Status semantics contradict each other: chat-built agents are Draft yet triggers fire them; build notes say "draft — activate it" while the automation is enabled; mobile BuildCard says "your new helper is live" | P2 | observed | Alex, Morgan | FEAT-005/016, JRN-1/2 | Three different truths shown for one object | createAgent defaults status Draft; fireTriggerInner/selectAgent never check status; client copy claims live | Missing status contract across layers [observed] | EV-022, EV-028, EV-011 | Users cannot form a correct model of what will run | Define and enforce one lifecycle (e.g. Draft = won't fire, chat-builds land Active-with-gates); align server notes + client copy | server/agents.mjs, server/triggers.mjs, apps/mobile BuildCard, web equivalent | ISS-001 | A Draft agent's trigger does not fire (audited skip) OR chat-built agents are created Active; copy matches behavior everywhere | Unit test on tick/fire with Draft target; copy review | open |
| ISS-008 | Recurring gated sends can expire forever without any alert: the "agent keeps failing" notification keys on consecutive `failed` runs; `expired` runs never trip it; approval pushes are no-ops without registered tokens | P2 | code-traced (observed park; expiry not waited) | Alex | FEAT-010/019/022 | Nothing — that is the symptom | expired ≠ failed in finishFailed's alert path; pushApprovalNotification no-op without tokens | Alert taxonomy gap [inferred from code, high confidence] | EV-007, EV-013, EV-026 | A daily briefing that parks every morning at 7:00 and expires at 7:30 never notifies anyone, indefinitely | Count expired runs in the consecutive-failure alert; add an in-app notification on every scheduled-run expiry | server/engine.mjs | ISS-004 | Two consecutive expired runs for one trigger produce the Owner in-app alert | Clock-shifted harness test | open |
| ISS-009 | Trigger lastStatus is recorded at fire time ("queued"/"started") and never updated to the terminal status — Automations UI shows stale state | P3 | observed | Morgan | FEAT-006/021 | Automation row says queued after the run completed | fireTriggerInner patches lastStatus once, before the run finishes | No terminal-status writeback [observed] | EV-011 | Minor, but compounds the false-success picture on the Automations screen | onRunFinished hook (or fire path) patches trigger.lastStatus with the terminal run status | server/triggers.mjs, server/engine.mjs | none | After a fired run completes/fails, trigger.lastStatus reflects it within one sweep | Harness assertion post-fire | open |
| ISS-010 | Read-only status passes execute live web.search with a meaningless query (the step's own description), burning a real external call per scheduled fire | P3 | observed | — | FEAT-007 | — | web.search executed with query "Read current data via Web Search & Reading." | deterministicFill backfills `query` from step detail for the auto-generated readonly plan [observed] | EV-011 | Wasted calls, noisy run traces; masks the emptiness of status passes | Skip query-bearing read tools in buildReadonlyPlan, or require a meaningful query source | server/orchestrator.mjs, server/engine.mjs | ISS-001 supersedes in practice | Status-pass runs contain no tool call with a self-referential query | Harness assertion | open |
| ISS-011 | Chat proposes builds to roles that cannot build: Guest/Helper receives a full build card, then /api/assistant/build 403s | P3 | observed | Sam | FEAT-001/004, JRN-4 | "I'll set this up" card → "Only an Owner or Adult Admin can build helpers." | assistantRespond has no role awareness for build proposals | Prompt/context omits the caller's build authority [observed] | EV-016 | Dead-end UX for guests/children | Include the caller's role capability in ASSISTANT_SYS context; answer with "ask a parent/admin" instead of a build card | server/planner.mjs | none | A Guest chat request for an automation yields an answer directing to an admin, not a build card | Persona pass re-run | open |
| ISS-012 | SECURITY (privilege escalation): `POST /api/runs/start` forwarded a client-supplied `sourceRef` verbatim, so any Limited Member could name any agent and inherit that agent's standing send consent | P0 | observed | Sam, Riley | FEAT-009/014 | none (API-level) | Client-named agentId accepted as the acting-agent identity for consent checks | WP-005 introduced standing per-agent send rights; the run-start route trusted client-declared agent identity [observed, found by mandatory xhigh adversarial review of WP-005] | implementation slice-log (security review), server/test/notify-contact-delivery.test.mjs | Any household member could send to any consented address as a privileged agent | Server-assign agent identity only; never accept it from the client | server/index.mjs | WP-005 | Client-supplied sourceRef.agentId cannot influence consent resolution; pinned by regression test | npm test (notify-contact-delivery suite) | fixed-this-run |
| ISS-013 | SECURITY (consent laundering): changing a contact method's address reset verification but PRESERVED the agent allowlist, letting a lower-privileged member repoint an adult-granted consent to an address they control | P0 | observed | Sam | FEAT-014 | Contact method keeps "allowed agents" after its address changes | allowlist not cleared on value change | Address mutation treated as an edit, not a re-consent event [observed, adversarial review] | implementation slice-log, notify-contact-delivery suite | Adult-granted send consent could be redirected to an attacker-chosen address | Clear the agent allowlist whenever the address changes; require re-grant | server/store.mjs / index.mjs contact-method patch | WP-005 | Changing value clears allowedAgentIds and verification; pinned by test | npm test | fixed-this-run |
| ISS-014 | SECURITY (missing adult gate): `allowedAgentIds` was the only contact-method field not adult-gated, on create OR patch | P0 | observed | Sam | FEAT-014 | A Child could grant an agent send rights | No role floor on the allowlist field | Field omitted from the adult-gating check [observed, adversarial review] | implementation slice-log, notify-contact-delivery suite | Minors could authorize unattended external sends | Adult-gate allowlist writes on both create and patch | server/index.mjs | WP-005 | Non-adult allowlist writes 403; pinned by test | npm test | fixed-this-run |
| ISS-015 | SECURITY (stale grants + policy bypass): deleting an agent did not revoke its contact-method grants, and an unresolvable `agentId` SKIPPED policy enforcement entirely (fail-open) | P1 | observed | Riley | FEAT-014 | none | Dangling allowlist entries; unknown agentId bypassed the allowlist check | Missing revocation on delete; unresolved-agent branch defaulted to allow [observed, adversarial review] | implementation slice-log, notify-contact-delivery suite | Deleted/unknown agent identities could send unattended | Revoke grants on agent delete; fail CLOSED on unresolvable agentId | server/agents.mjs, server/notify.mjs | WP-005 | Agent delete clears grants; unknown agentId denied; pinned by tests | npm test | fixed-this-run |
| ISS-016 | SECURITY (missing role floor): `/api/notify` had no role floor and treated the agent allowlist as optional | P1 | observed | Sam | FEAT-014 | none | Any role could invoke notify; allowlist optional | Route predates the standing-consent model [observed, adversarial review] | implementation slice-log, notify-contact-delivery suite | Unauthorized external sends via the HTTP surface | Add role floor; make allowlist mandatory when an agent is acting | server/index.mjs | WP-005 | Sub-floor roles 403; allowlist enforced; pinned by tests | npm test | fixed-this-run |
| ISS-017 | `normalizePlan` was NOT on every path into a run: `buildPlanFromSkill` (deterministic skills) and raw plans posted to `POST /api/runs/start` bypassed it, so a toolless "Send email" step STILL reported `succeeded` — the headline bug survived behind a fully green server suite | P0 | observed | Alex | FEAT-003/009 | Skill-run briefing reports success with nothing sent | Toolless send step succeeded on the skill path | Normalization applied at one entry point instead of the shared choke point [observed — found ONLY by the mandatory browser pass, DEC-A03 reversal condition] | implementation slice-log (browser pass), tests/topgun/web/run-honesty.spec.ts, false-success-regression suite | The user's briefing runs AS A SKILL, so the fix would have missed the actual reported scenario | Enforce normalization at `startRun`, the single choke point every run passes | server/orchestrator.mjs (startRun) | WP-003 | Raw-plan and skill-path toolless sends cannot report succeeded; pinned by 2 tests | npm test + web/run-honesty.spec.ts | fixed-this-run |
| ISS-018 | `deliverViaChannel` email branch never consulted the household kill switch — harmless while email was HTTP-only, live once WP-005 made that path schedulable | P1 | observed | Riley | FEAT-014 | Kill switch would not stop scheduled email | Kill-switch check absent on the email branch | Pre-existing gap the audit flagged; promoted to live risk by WP-005 [observed] | implementation slice-log | Kill switch (a core safety control) would not have stopped unattended sends | Consult the kill switch in the email branch | server/notify.mjs | WP-005 | Kill switch blocks email delivery; pinned by test | npm test | fixed-this-run |
| ISS-019 | Residual MEDIUM risks accepted, not silently dropped: (a) a household cannot re-tighten `notify_contact` to require per-send approval once standing consent is granted; (b) LLM-threaded message bodies mean externally-sourced text can ship under the family's identity; (c) third MEDIUM finding recorded in the slice log | P2 | observed | Riley | FEAT-014 | — | — | Consequences of the standing-consent model introduced by WP-005 [observed, adversarial review] | implementation slice-log (security review section) | Consent, once granted, is coarser than some households may want; prompt-injection surface for outbound text | Follow-on work: per-agent "require approval" toggle; provenance labeling of LLM-composed outbound bodies | server/notify.mjs, server/index.mjs | out of selected scope | — | — | accepted-residual |
