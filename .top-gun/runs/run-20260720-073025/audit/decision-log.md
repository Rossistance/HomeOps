# Decision and Rationale Log — run-20260720-073025

Audit-phase decision PROPOSALS (DEC-A## — orchestrator ratifies; prior-run DEC-08/DEC-10 cited as lineage only).

| DEC-ID | Decision | Alternatives | Why preferred | Constraints/evidence | Preserved strengths | Trade-offs | Deferred | Reversal condition |
|---|---|---|---|---|---|---|---|---|
| DEC-A01 | Produce runtime evidence on ISOLATED harness servers (real server/index.mjs, temp data dirs, scripted fake provider) instead of the resident dev-stack tenant | (a) drive the live local tenant with TG- records; (b) code-only audit | Zero risk to REAL family data; same code path (harness boots the actual server); deterministic model behavior isolates the seams from model variance | Brief's data-safety limits; harness is the established pattern (assistant-persistence.test.mjs) | The engine, gates, and registries were exercised as-is | The exact text a real cloud model would emit is not covered (HYP-005) | Live-provider transcript check | If a real-model transcript shows materially different build JSON that dodges ISS-001 (structurally impossible) — would reopen |
| DEC-A02 | Treat ISS-001/002/004 (+003) as ONE convergent root-cause cluster: "the chain is real at the tool layer but four seams convert non-delivery into success" — not per-tool config bugs | Rank stale prod deploy (d) as primary; or per-connector setup gaps | Single-cause parsimony matches "every single type of task"; version diff refutes (d) as cause; repros show the seams fire with everything 'configured' | EV-011..EV-015, EV-018 | notify.mjs + approvals stay honest and untouched | — | — | A live-model repro at HEAD that delivers real email end-to-end would falsify |
| DEC-A03 | Skip live-UI browser persona journeys this run; persona passes ran at API level against the harness | (a) web browser journeys on the local stack (TG- records + cleanup); (b) reopen native lane | The decisive seams are server-side and fully evidenced at the authority layer; native lane CLOSED by user (DEC-10 lineage, binding); web journeys add screenshot texture but no verdict-changing signal at material budget/data-risk cost | Budget 350k soft; real family data in the live tenant; both clients are thin observers of the same routes (EV-021/022/025) | — | Track A visual evidence is thinner: no screenshots this run (disclosed in ledger + report §25); FEAT-016/017 blocked, FEAT-018 partial | One web journey with TG- records as a follow-up check | If the fix phase touches client rendering, a browser pass becomes mandatory before ship |
| DEC-A04 | Recommend the fix bundle WP-001+002+003+004 (wire skill→trigger, 7 AM anchor, truthful summaries, parked-run honesty) with WP-005 (real registry delivery tool) as the follow-on requiring a product/consent decision | (a) WP-005 first ("just make email work"); (b) only WP-003 ("just stop lying") | 1–4 makes every chat-built agent actually run its skill at the right time and never misreport — the user's stated 'done' — without new external-send authority; WP-005 changes consent surface (standing per-agent send rights) and deserves its own explicit selection | Mandate: smallest coherent cross-layer correction; DEC-08 lineage caution on Google | Approval model, registry gates, kill switch untouched by 1–4 | Unattended delivery still needs WP-005 + a connected Google account | WP-005, WP-006 | If the user says unattended delivery is THE priority, promote WP-005 into the bundle |
| DEC-006 | **ORCHESTRATOR DECISION (not a proposal) — USER SELECTION, 2026-07-20T17:35Z: implement the FULL menu — recommended bundle WP-001+002+003+004, PLUS WP-005 (real unattended delivery), PLUS WP-006 (surface truth polish).** User's words: "Recommended bundle plus item 5, and item 6 (deploy blocker fixed) — consent granted for wrhixon@gmail.com" | Bundle-only (1–4), deferring 5 and 6 as DEC-A04 recommended | User explicitly promoted WP-005 into scope, which DEC-A04 anticipated ("If the user says unattended delivery is THE priority, promote WP-005 into the bundle"); unattended delivery IS the user's stated goal (a 7 AM email that arrives without them approving each morning) | Selection recorded at the SELECTION_PENDING gate; matching guide ART-019 routes all six; planned budget 320k + 55k reserve | Approval model, kill switch, registry gates remain — WP-005 adds standing per-agent consent, it does not remove gating | Larger scope than the recommended bundle; WP-005 carries the highest risk and gets mandatory xhigh adversarial review per the guide | Nothing deferred — full menu selected | User narrows scope mid-implementation |
| DEC-007 | **ORCHESTRATOR DECISION — LIVE EMAIL AUTHORIZATION: explicit user consent granted for real sends to wrhixon@gmail.com** (the user's own address, the briefing recipient). This unblocks WP-005 acceptance with a genuine delivery and lifts the audit's blocker #2 for THIS recipient only. | Mocked-Gmail proof only (the prior default lane) | The user granted consent in the same message as the selection, in their own words; a real arrival at the real address is the only evidence that actually answers "the email never comes" | Consent is RECIPIENT-SCOPED: wrhixon@gmail.com only. No other family contact method may receive a live send. Mocked/harness proof still comes FIRST; the live send is final acceptance, not the primary lane. DEC-08 lineage (stale household Google account) may make a live send fail for account reasons — that is an honest blocker to report, never to paper over | Registry fail-closed behavior (verified EV-017) is preserved — consent adds an authorized path, it does not disable the gates | The stale Google account may block live proof; observation depends on the user confirming receipt (the session's Gmail MCP is bound to budgetbeacon.ai@gmail.com, NOT wrhixon@gmail.com — Claude cannot see that inbox) | Live sends to any other address | User revokes consent, or a send misfires to a non-consented recipient (halt immediately) |
| DEC-008 | **ORCHESTRATOR DECISION — production verification UNBLOCKED (audit blockers #1/#3 cleared).** User repaired the Render deploy; verified independently this session, not taken on trust | Continue treating prod as unverifiable | Firsthand evidence: served bundle flipped `index-Cu2Tvfl5.js` → `index-n6sejv4F.js`; GitHub deployment records show `c0c4596` deployed 2026-07-20T17:29–17:31Z; `/api/health` 200 `env:production`; `c0c4596` == local HEAD | Prod now runs HEAD **without** the fixes — post-implementation prod proof requires a NEW deploy (commit+push, which auto-deploys again) | The no-prod-behavioral-probe policy still stands for live family data; health + build-identity checks only, unless the user authorizes more | A prod journey re-run still needs the user's go-ahead for anything beyond health/identity | Post-fix prod verification until after deploy | Autodeploy breaks again (re-check deployment records before any prod claim) |
| DEC-009 | **ORCHESTRATOR — `validate_memory --strict` FAILS and is accepted as a TOOL limitation, not forced to pass.** The failure is reported openly in every summary; no ledger row was hand-edited to silence it | (a) hand-edit/delete the ART-022 ledger row; (b) claim "all validators pass" | The ledger is CORRECT: ART-025 supersedes ART-022 for the same live file, and ART-025's recorded hash `1b9b95b61bad` matches the on-disk sha256 exactly (orchestrator verified firsthand). `validate_memory` is not supersede-aware, so it re-checks the superseded row forever. The mem contract makes the ledger append-only and says reference-mode drift "must be reported as drift, never hidden" — editing it to get a green check would be the actual violation | Orchestrator recomputed the hash independently; implementation lead journaled a disagreement (#53) with a suggested tool fix | Append-only ledger integrity preserved | This run cannot show a clean `--strict` memory validation; every report must state that plainly | Tool fix: teach validate_memory to skip superseded rows | Tool gains supersede awareness → re-run should pass with no data change |
| DEC-010 | **ORCHESTRATOR — the 4 failing `core-flows` web tests are PRE-EXISTING, proven by experiment rather than argument.** They are recorded as failures and counted as passes nowhere | Accept the lead's file-identity argument; or treat them as regressions and loop | Discriminating check run firsthand: stashed ONLY the implementation's `src/` changes, re-ran `core-flows.spec.ts` at clean HEAD → **the identical 4 failures** (chromium calendar; webkit-iphone nav, command palette, calendar). Working tree then restored and verified byte-identical to a pre-stash patch backup | Orchestrator experiment this session; `src/screens/Calendar.tsx` and `src/components/Shell.tsx` also byte-identical to HEAD | The implementation neither caused nor fixed them | **Correction to the lead's report: the honest count is 4 failures / 12 passed, not 3 / 13** — the lead's tally was off by one | Fixing core-flows (its own harness/app mismatch, like the smoke fix earlier this session) | A core-flows failure appearing that does NOT reproduce at clean HEAD would be a real regression |
| DEC-011 | **ORCHESTRATOR — 9 inert `TG-Web honesty check` run records left in the local dev tenant; NOT deleted** | Hand-delete from the SQLite audit store | Runs are immutable audit history with no delete route; hand-editing a live audit store to tidy test rows is worse than nine clearly-labelled inert rows. Read-only audit confirmed 0 TG- agents, skills, triggers, or contact methods — nothing else leaked | Implementation lead's cleanup audit; orchestrator accepts | Audit-trail immutability preserved | Nine labelled rows visible in the local dev tenant's run history (local only — never production) | — | A supported bulk-delete route for test records would allow cleanup |
| DEC-A05 | Foreign journal/ledger rows (#2–#3, ART-001) from concurrent run-20260720-072344 are preserved and ignored as foreign; no disagreement event needed | Journal a disagreement; attempt cleanup | Rows are append-only-protected; orchestrator already documented the collision (journal #7); no foreign writes touched audit/ register FILES in this run's tree (verified: init_audit created them fresh this session) | Journal #7; brief's collision protocol | — | ART numbering in this run starts at ART-004+ | — | Any foreign write appearing under this run's audit/ would trigger the disagreement protocol |
