# Capability-Task Matching Guide

- Run: run-20260719-073933 (FamiliOS / homeops-ai)
- Author: matching-lead, 2026-07-19 (UTC)
- Sources: audit/implementation-queue.md (WP-001..005), audit/issue-register.md (ISS-001..010+003b), master-report.md §17–21, functionality-inventory.md coverage notes, hypothesis-queue.md (HYP-001..006), facts-and-notes.md (revalidated), budget policy = top-gun:lean-implementation references/budget-policy.md (canonical).
- Context gate: `product-management:write-spec` invoked and loaded this session — its structure (problem, users, scope, Given/When/Then acceptance) is applied through each task's WP linkage and Verification cell. `design:design-handoff` invoked and loaded — token-level design grounding applied (see note below). Neither skill was unavailable; no playback fallback needed.
- Design grounding correction (design-handoff discipline, tokens not values): DESIGN_SYSTEM.md fixes status vocabulary at `coral` = attention/danger and `amber` = warning (lines 49–50; "status colors are vocabulary", line 20). WP-002's draft direction said "amber = attention"; the degraded-mode banner is a warning state → **amber is correct for the banner**, while T-06's provider_error rendering uses **coral**. Banner must carry `role="status"`; attention-tile pattern (line 125) is the precedent for warm-accent surfacing.

## Capability Map

Inventory of THIS session (matching-lead), evidence-backed, probed 2026-07-19T09:04–09:08Z. The implementation lead re-inventories at dispatch — deferred-tool sets and model enums can differ per session.

| Capability | Classification | Evidence | Side-effect authority | Timestamp (UTC) |
|---|---|---|---|---|
| File tools: Read, Grep, Glob, Edit, Write | use now | Prompt-visible; exercised throughout this session | local-write (repo + run dir) | 2026-07-19T09:05Z |
| Bash + PowerShell shells | use now | Prompt-visible; executed probes and mission scripts this session | local-write + local process control | 2026-07-19T09:05Z |
| Python 3.14 (mission scripts: mem, matching validators) | use now | init_matching.py and append_event.py executed OK this session | local-write (run dir only) | 2026-07-19T09:04Z |
| Skill tool: product-management:write-spec, design:design-handoff | use now (used) | Both invoked this session; skill bodies loaded | read-only guidance | 2026-07-19T09:04Z |
| Skill catalog: verify, code-review, feature-dev:feature-dev, superpowers:test-driven-development, design:accessibility-review, engineering:* | use if needed | Session available-skills listing | read-only guidance (verify/run drive the local app) | 2026-07-19T09:05Z |
| Agent tool subagent types: general-purpose, Explore, Plan, claude, feature-dev:code-architect/explorer/reviewer, pr-review-toolkit:silent-failure-hunter/pr-test-analyzer/code-reviewer, code-modernization:test-engineer/security-auditor, code-simplifier | use now | Agent-types list present in session context | delegated — inherits per-brief authority | 2026-07-19T09:05Z |
| Model aliases for subagents: sonnet, opus, haiku, fable (per-call override); reasoning effort set via agent definition/brief, not per-call | use now | Agent tool schema model enum observed in this session; session model is claude-fable-5 | n/a | 2026-07-19T09:05Z |
| Playwright MCP emulator (plugin_playwright: navigate, snapshot, resize, screenshot, click, type, fill_form, console, network) | use now | Deferred-tool list + ToolSearch schema-load succeeded for 5 core tools this session; entire audit ran on it (facts-and-notes) | local browser control, localhost origins only | 2026-07-19T09:06Z |
| chrome-devtools MCP (perf traces, a11y snapshots, lighthouse) | use if needed | Deferred-tool list presence; not schema-loaded (no current need) | local browser control | 2026-07-19T09:06Z |
| Claude_Browser pane (mcp__Claude_Browser__*) | use if needed — degraded, revalidate first | Prompt-visible; tabs_context returned a seed tab on http://localhost:5173 this session; audit session recorded screenshot hangs + origin-approval denials with no interactive user | local browser control | 2026-07-19T09:07Z |
| claude-in-chrome MCP | blocked (unverified) | Deferred list; requires user's Chrome + connected extension; not probed — connection state unknowable without side effects | user-browser control | 2026-07-19T09:06Z |
| computer-use MCP | blocked | Deferred list; first use requires interactive request_access approval — an auth-like flow this mission must never trigger | desktop control | 2026-07-19T09:06Z |
| claude.ai connector MCPs (Gmail, Calendar, Notion, Drive, Figma, Vercel, +33 more) | blocked | Session notice: servers require authentication; non-interactive session cannot OAuth | external-write if ever authed | 2026-07-19T09:05Z |
| WebFetch / WebSearch (deferred) | use if needed | Deferred-tool list; loadable via ToolSearch | external read-only | 2026-07-19T09:05Z |
| Repo scripts: npm test (node --test server/test, 287 tests), typecheck (tsc), build (tsc + vite build), dev (scripts/dev.mjs), server (node server/index.mjs) | use now — with ISS-001 caution | package.json read this session; audit baseline typecheck PASS + 287/287 PASS. CAUTION: until WP-001 lands, npm test beside a live backend corrupts server/.data (ISS-001) | local-write (repo artifacts + .data risk) | 2026-07-19T09:05Z |
| Local runtime: backend :8787 (v1.2.0, node v25.8.2, authRequired, externalActionsEnabled), web :5173 (Vite dev), browser runtime :9223 | use now | Read-only health probes this session: api ok:true 200, web 200, :9223 200 — drift check vs audit PASSED | local app-state writes via normal in-app use | 2026-07-19T09:05Z |
| Known-good backend restart: node server/index.mjs (background; honors HOMEOPS_DATA_DIR) | use now | audit/evidence/backend-restart-log.txt + DEC-001 (restart executed and proven during audit) | local process control | 2026-07-19T09:06Z |
| git (read-only: log, diff, status) | use now | Repo at main, HEAD ffcfa33, one pre-existing dirty file (.claude/launch.json) per audit; mutations barred by authority | read-only by mandate | 2026-07-19T09:05Z |
| GitHub Actions CI (.github/workflows/ci.yml) | blocked for execution | Runs only on push; git mutations barred — CI changes verifiable locally only by simulation | external | 2026-07-19T09:05Z |
| Render production deployment | blocked | No deploys authorized; no confirmed live URL (HYP-006 open) | external | 2026-07-19T09:05Z |
| Nodes installed locally (nvm: 20.20.2, 22.12.0, 25.8.2) | use now (constraint) | facts-and-notes (revalidated risk stands): none satisfies engines '>=22.13 <25' — the reconciled-pin boot smoke in T-07 cannot run locally today | local runtime | 2026-07-19T09:05Z |
| Artifact publishing, mcp__visualize widgets, ccd_session chips | irrelevant | Prompt-visible but no mission task produces claude.ai artifacts or inline visuals | n/a | 2026-07-19T09:05Z |
| Stack-mismatched plugin MCPs (Prisma, SAPUI5, Twilio, appstore, Mapbox, etc.) | irrelevant | Deferred list; repo is dependency-free Node + Vite/React — no surface for them | n/a | 2026-07-19T09:05Z |

## Phase Matrix

| Phase | Capabilities engaged | Explicitly not engaged (why) |
|---|---|---|
| MATCHING (this phase, closing) | Read/Grep/Glob on registers; python mem + matching scripts; Skill (write-spec, design-handoff); ToolSearch probes; read-only curl health checks | Emulator drive (routing needs register evidence, not new product interaction); repo Edit/Write (read-only phase); subagents (single-lead policy, no hard evidence need arose) |
| SELECTION_GATE | Orchestrator presents master-report §21 menu + this guide's scenario ledger to the user | All implementation tooling (hard gate: no product-source writes before selection) |
| IMPLEMENTATION waves 0–2 (T-01..T-09, T-12) | Edit/Write/Read/Grep on repo; npm typecheck always, npm test only per T-01 protocol; Playwright MCP for in-slice validation; Agent tool (sonnet implementers); mem scripts; git read-only | Connector MCPs (no task needs them — never force a plugin); computer-use/claude-in-chrome (blocked; Playwright suffices); git mutations (user-gated); npm install of new deps (only removal in scope, T-09) |
| VERIFICATION + COVERAGE wave 3 (T-10, T-11, T-13) | Playwright MCP journeys; backend restart with temp HOMEOPS_DATA_DIR; sha256 + health checks; opus verification agent; verify skill; pr-review-toolkit:silent-failure-hunter | Production probes (unless user authorizes the single HYP-006 read-only GET); Claude_Browser pane (unless revalidated interactive); parallel emulator sessions (runtime is a serialized resource) |
| COMPLETION | mem validate_memory --strict; snapshot registration; budget-ledger reconciliation; final report | Anything side-effectful |

## Task Matrix

Bounded tasks decomposed from WP-001..005 (write-spec discipline: each row inherits problem/users/scope from its WP entry and carries testable acceptance in Verification), plus the four blocked persona journeys and the ISS-008 CI guard. Budgets are soft output-token guidance per the canonical tables.

| Task | IDs | Primary route | Fallback route | Agent role | Model | Effort | Budget | Concurrency group | Checkpoints | Drift signals | Verification | Return contract |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| T-01 Test isolation: store test-context guard, harness test-process env, stall-test rewrite through spawned-server ctx | WP-001; ISS-001, ISS-008; FEAT-166 | Inline impl-lead: Edit server/test/harness.mjs + engine-hardening.test.mjs + store.mjs guard; node --test; sha256 server/.data pre/post | Delegate code-modernization:test-engineer (sonnet) if lead saturated | inline (impl lead) | inherit | medium | 45k soft | W0 solo — exclusive owner of npm test + .data surface | 2 (guard lands; rewrite lands) | Any write outside the 3 named files + ci.yml; running the suite without pre-hash; pass claims without hash+health output | With dev backend live: npm test passes, .data hash byte-identical, /api/health stays 200, stall assertion still proves the sweeper | Delta + diff summary + hash/health transcript + journal events |
| T-02 Degraded-mode surfacing: global banner (role=status, amber=warning token) in mobile shell, failure-vs-empty tile copy, PIN-copy + subtitle-fragment fix | WP-002; ISS-002; FEAT-001, 004, 005, 016 | Delegate general-purpose sonnet: Edit Shell/Onboarding/Lock/Dashboard/useStore; Playwright MCP backend-down run | Inline impl-lead with design:accessibility-review consult | general-purpose (same agent as T-03, serialized) | sonnet | medium | 50k soft | G2 frontend-store (serial with T-03) | 2 (banner; copy pass) | Redesign creep; new dependencies; banner missing role=status; touching server code | Emulator with backend stopped: explicit unreachable state at each failing step + dashboard degraded banner announced via role=status; empty-vs-failure copy distinct | Delta + inspected before/after screenshots + journal |
| T-03 Local/sample client-only sessions: mint local session for sample/create modes, Lock merges + labels local households, claimed-flag routes Create-household to email signup | WP-002; ISS-003, ISS-003b; FEAT-003, 006, 146 | Same agent as T-02 (serialized): useStore session model + Lock/Onboarding edits; Playwright claimed-server reseed run | Respawn to opus per respawn protocol if 150% checkpoint overrun or two failed attempts | general-purpose (same agent as T-02) | sonnet | high | 60k soft | G2 frontend-store | 2 (session model; Lock merge) | Touching server claim semantics in index.mjs (out of scope); hiding local households; auth-path regressions; scope creep into T-08's payload | Emulator: reseed on claimed server lands working Harper dashboard; locally-created household visible and labeled On-this-device; email-signup path still green | Delta + screenshots + IndexedDB evidence + journal |
| T-04 Assistant local fallback: route no-provider chat through lib/ai.ts engine, scoped provider-only prompts, chip alignment, local-vs-provider answer labels | WP-003; ISS-004; FEAT-020, 021; OPP-1 | Delegate general-purpose sonnet: Assistant.tsx + ai.ts edits; Playwright no-provider chat run | feature-dev:feature-dev skill-guided route (same agent class) | general-purpose (same agent as T-05, serialized) | sonnet | medium | 45k soft | G3 assistant (serial with T-05) | 2 (routing; chips+labels) | Provider-path regressions; chips promising beyond local-engine capability; unlabeled answer provenance | Emulator no-provider: planning chip returns a local approvable plan or a scoped provider prompt — never a bare dead-end; provider path unchanged | Delta + screenshots + journal |
| T-05 Rules-engine trigger fix: apply detectTrigger() in the automation-creation path, show resolved trigger in plan preview | WP-003; ISS-005; FEAT-042; HYP-005 | Same agent as T-04: locate the wiring gap in Automations.tsx, fix, rebuild automation in emulator | Inline impl-lead | general-purpose (same agent as T-04) | sonnet | medium | 20k soft | G3 assistant | 1 (midpoint) | Hardcoding Schedule instead of deriving; unnecessary edits to ai.ts detection logic | Emulator: building Every-morning-at-7am automation yields trigger=Schedule in preview AND stored record (HYP-005 resolved observed) | Delta + screenshot + stored-automation evidence + journal |
| T-06 Weather executor honesty: r.ok + j.current validation, provider_error return, coral=attention rendering in tool-result panel | WP-004; ISS-006; FEAT-062; HYP-003 | Inline impl-lead: mirror sibling RSS pattern in server/connectors.mjs ~L397-402; capture upstream fetch status (HYP-003); emulator run | Delegate general-purpose haiku (near-mechanical sibling-pattern mirror) | inline (impl lead) | inherit | medium | 20k soft | G4 server-connectors (parallel-safe in wave 1) | 1 (midpoint) | Inventing a new error taxonomy instead of mirroring siblings; skipping the emulator check | Forced bad upstream shows visible provider_error (never hollow success); valid upstream shows temperature; server test added; HYP-003 upstream status logged | Delta + tool-result screenshot + test output + journal |
| T-07 Node story reconciliation: render.yaml NODE_VERSION pin, engines range reconcile, README fix, CI node alignment | WP-005; ISS-007; FEAT-166, 167; HYP-006 | Inline impl-lead: decide pinned version inside a reconciled engines range, then config/doc edits | Delegate general-purpose sonnet | inline (impl lead) | inherit | medium | 20k soft | G5 config (serial with T-09, T-12 — shared package.json + ci.yml) | 1 (after decision) | Pinning a version node:sqlite is untested on; leaving README contradicting engines; touching runtime code | Consistency check passes: render.yaml pin inside engines range and equal to CI node; local boot smoke deferred (no in-range Node installed — see Unavailable Effects) | Delta + diff + consistency table + journal |
| T-08 Pre-auth roster minimization: trim /api/profiles payload (drop relationship + age hints), contract coordinated with T-03 Lock | WP-005; ISS-009; FEAT-005, 160; HYP-004 | Inline impl-lead (wave 2, after G2 lands): server/index.mjs payload trim; unauth curl before/after | Delegate general-purpose sonnet with the payload contract pre-agreed in brief | inline (impl lead) | inherit | high | 25k soft | G6 server-api (after G2) | 1 (contract agreed) | Breaking Lock roster UX; adding auth schemes beyond scope; leaving age/relationship fields in payload | Unauth curl on claimed tenant returns no relationship/age PII; Lock still renders; HYP-004 resolved observed; 287-suite green via T-01-safe protocol | Delta + curl transcripts + journal |
| T-09 Remove unused react-router-dom production dependency | WP-005; ISS-010; FEAT-166 | Inline impl-lead: re-confirm zero imports (grep), remove dep, npm run build + bundle inspect | Delegate haiku XS agent | inline (impl lead) | inherit | low | 6k soft | G5 config (after T-07's package.json edit) | return only | Any src/ edits (removal only); adopting URL routing (separate decision, out of scope) | npm run build green; bundle inspection shows no react-router-dom | Delta + build output + journal |
| T-10 Coverage completion A: unclaimed-server env flip (temp HOMEOPS_DATA_DIR, known-good restart), HYP-001 resolution, JRN-2 Alex populated-power morning, JRN-3 Morgan invite-and-approve, at 375x812 | JRN-2, JRN-3; HYP-001; ISS-003-family regression check | Delegate general-purpose sonnet: Playwright MCP journeys with screenshot-inspect pairing; backend restart against temp data dir; restore afterward | Claude_Browser pane only if Playwright fails twice for the same cause (revalidate its origin gate first); chrome-devtools MCP for spot a11y/perf | general-purpose | sonnet | medium | 60k soft | G7 runtime-exclusive (serial T-10 then T-11; sole emulator + backend user) | per journey | Leaving backend on temp dir after finish; uninspected screenshots; role-gating claims without runtime proof; touching product source | JRN-2/3 step tables with paired evidence; HYP-001 resolved observed (does unclaimed server clear the ISS-003 family?); backend restored to default .data with health 200 | Delta + evidence files (audit/evidence naming) + coverage note + journal |
| T-11 Coverage completion B: JRN-4 Lily child-view fencing + JRN-5 Elaine grandparent Calm-Mode runtime journeys, coverage recompute | JRN-4, JRN-5; role-gating FEAT subset | Delegate general-purpose sonnet on the T-10 environment (serialized handoff) | Same fallback ladder as T-10 | general-purpose | sonnet | medium | 55k soft | G7 runtime-exclusive | per journey | Child-view permission leaks observed but unreported; coverage recomputed without evidence links | JRN-4/5 executed live: child and grandparent gating observed at runtime; coverage numerators recomputed with evidence IDs | Delta + evidence + updated coverage numbers + journal |
| T-12 CI guard for the ISS-001 class: workflow job boots server, runs suite, asserts .data byte-identical (ISS-008 reproduction gate) | ISS-008; WP-001 slice 4; FEAT-166 | Inline impl-lead: ci.yml job + local bash simulation of the job's exact steps | Delegate general-purpose sonnet | inline (impl lead) | inherit | medium | 25k soft | G5 config (strictly after T-01) | 1 (job scripted) | Claiming CI verified end-to-end (impossible pre-push); breaking existing jobs; YAML drift into unrelated workflows | YAML valid; job steps executed locally as a script pass with .data hash equality; end-to-end CI proof explicitly deferred to first user push | Delta + local simulation transcript + journal |
| T-13 Final verification + integration: re-execute every selected WP acceptance criterion, security review of T-08, silent-failure review of T-02/T-06 error surfaces, ledger reconciliation | All selected WPs; ISS-001..010+003b; HYP-001, 003, 004, 005 | Delegate fresh general-purpose opus: verify skill + hash/health/emulator re-checks + pr-review-toolkit:silent-failure-hunter sub-pass on error-handling diffs | claude agent type on fable (session top tier) at xhigh if opus dispatch unavailable at execution time | general-purpose (independent reviewer — no self-review of own diffs) | opus | xhigh | 60k soft (reserve-funded) | W-final solo (exclusive) | 2 (P0/P1 re-checks; review pass) | Rubber-stamping without re-running checks; verification skipped to save tokens; fixing findings silently instead of filing them | Every acceptance criterion re-executed with fresh evidence; discrepancies filed as issues + journal disagreement events, never silently patched | Verification report + reconciled budget ledger + journal completion event |

### Route comparisons — considered alternatives and rejections (scoring per matching-policy rubric)

- **T-01**: code-modernization:test-engineer (task fit 4, purpose-built for pinning behavior with tests) vs inline lead (fit 4, fewer moving parts, and wave 0 is deliberately solo so delegation buys zero parallelism). Tie-breakers → inline. Kept as fallback.
- **T-02/T-03 split across two parallel agents**: rejected on collision risk (both own useStore.ts, Lock.tsx, Onboarding.tsx — shared-state contention scores 1/5); one agent, serialized slices, wins despite slower wall-clock (speed weight 1 vs collision weight 2).
- **T-03 model class**: opus (L-slice treatment per model table) considered because session/auth flow is touched. Rejected: the architectural decision is already made and recorded by the audit (mint local sessions; rejected alternative = forcing a server actor — master-report §19), so remaining ambiguity is M-class implementation. sonnet + high effort per tables, with the respawn protocol as the recorded escalation path. This is the single most escalation-likely row.
- **T-04**: feature-dev:feature-dev skill-guided route scores equal task fit; plain general-purpose with the WP's explicit slices has fewer moving parts. Skill route kept as fallback.
- **T-06**: haiku + low (mechanical sibling-pattern mirror, cost 5/5) vs inline-lead medium. Correctness risk decides it (weight 3): this is the flagship connector and the product's promise-integrity surface, and acceptance needs emulator + test judgment. haiku demoted to fallback.
- **T-08 model class**: opus considered (model table: frontier for security-sensitive changes). Resolved as sonnet-class implementation at high effort + mandatory opus xhigh security review inside T-13 (effort table: xhigh/max = security review). Rationale: the change is subtractive (removing fields), gated by curl evidence and the suite; the frontier spend lands where the table aims it — at verification.
- **T-10/T-11 emulator route**: Claude_Browser pane as primary was considered (it exists in this session with a seeded localhost:5173 tab — observed via tabs_context). Rejected: audit session recorded screenshot hangs and origin-approval denials with no interactive user, vs Playwright MCP proven across the entire audit (every EV screenshot) and schema-load verified this session. Safety/evidence-quality weights decide; Claude_Browser demoted to revalidate-first fallback.
- **T-12**: full end-to-end CI verification via push — safety veto (git mutations barred). Local simulation of the job's steps is the only honest verification; the residual gap is declared, not hidden.
- **T-13**: code-review skill route considered; verify skill wins primary because the contract is re-executing acceptance criteria end-to-end (observed behavior), not diff review. silent-failure-hunter is added as a sub-pass precisely because WP-002/004 are silent-failure fixes — reviewing them for new silent failures is purpose-fit.

## Model and Effort Rationale

Citations are to the canonical tables in top-gun:lean-implementation references/budget-policy.md.

- **Task classes** (budget-policy Task classes table): T-09 is XS (mechanical, single file, ≤8k). T-05, T-06, T-07, T-08, T-12 are S (single-surface, low ambiguity, ≤25k). T-01, T-02, T-04, T-10, T-11 are M (multi-file slice or journey batch, ≤60k). T-03 is M at the class ceiling (60k) — L treatment was declined because the audit pre-made the architectural decision; if execution reveals real cross-layer design decisions, that is a class upgrade requiring a DEC entry per checkpoint math. Nothing here is XL; WP-002 was decomposed precisely to avoid dispatching an XL.
- **Model classes** (Model selection table): sonnet carries all delegated S/M implementation and the persona journeys — the table names "persona journeys" and "S/M slices, tests" as sonnet-class work. haiku appears only as fallback for mechanical work (T-06 fallback, T-09 fallback) per "XS/S mechanical work". opus is assigned exactly where the table points frontier spend: "final verification of P0 scope, security review, adversarial verification of P1 claims" — that is T-13, which also carries the T-08 security review. inherit marks inline impl-lead work (parent model already fits, per the inherit row). Aliases verified in THIS session's Agent tool enum: sonnet, opus, haiku, fable; the implementation lead must re-verify the enum in their own session before writing briefs.
- **Effort ladder** (Reasoning effort table): low = T-09 (mechanical edit). medium = routine implementation and standard journeys (T-01, T-02, T-04, T-05, T-06, T-07, T-10, T-11, T-12). high = T-03 (cross-layer session flow) and T-08 (privacy-relevant payload decision). xhigh = T-13 (final verification of P0 scope + security review). Per the policy, raised effort is always paired with explicit evidence requirements in the Verification cell — effort buys depth, not correctness.
- **Mechanism note (honest)**: reasoning effort is not a per-call Agent-tool parameter in this session; it is carried by agent definitions and dispatch-brief instructions. Effort assignments here are binding brief content, not host-enforced knobs.
- **Escalation**: model/effort switches use the respawn protocol (budget-policy) — stop, correction event, salvage via delta, revise brief, respawn. Pre-identified likely case: T-03.

## Planned Budget Ledger

All budgets are SOFT. The host enforces no output-token cap on Agent calls in this session; marking any budget hard would be a fabricated control (budget-policy explicitly forbids that claim).

| Task | Budget (output tokens) | Soft/Hard | Rationale |
|---|---|---|---|
| T-01 | 45k | soft | M-class: 3 files, low ambiguity, verification-heavy (hash + health + suite) |
| T-02 | 50k | soft | M-class multi-file UI slice + emulator validation |
| T-03 | 60k | soft | M-class ceiling: session-flow risk, pre-made design decision |
| T-04 | 45k | soft | M-class: two files + emulator validation, some scoping latitude |
| T-05 | 20k | soft | S-class: single wiring gap + preview surface |
| T-06 | 20k | soft | S-class: sibling-pattern mirror + test + emulator check |
| T-07 | 20k | soft | S-class: config edits after one reconciliation decision |
| T-08 | 25k | soft | S-class ceiling: payload decision + T-03 coordination + curl evidence |
| T-09 | 6k | soft | XS-class mechanical removal |
| T-10 | 60k | soft | M-class ceiling: env cycle + 2 journeys + HYP-001 + restore |
| T-11 | 55k | soft | M-class: 2 journeys + coverage recompute |
| T-12 | 25k | soft | S-class ceiling: YAML + full local simulation |
| T-13 | 60k | soft | Reserve-funded verification (never spent on feature work) |

- Feature + coverage subtotal (T-01..T-12): 431k
- Reserve (T-13 verification 60k + 9k contingency): 69k = 16% of subtotal (inside the 15–20% band; reserve rule)
- **Mission total planned: 500k soft**
- Selection scenarios (menu §21): Recommended bundle 1+2+3 with coverage + CI guard (T-01..T-05, T-10..T-12) = 360k + 60k reserve = **420k**. Recommended bundle without coverage completion (T-01..T-05, T-12) = 245k + 45k reserve = **290k**. Full slate (option 6 + coverage + CI) = **500k**. Single-option math: option 1 = T-01+T-12 (70k) + reserve 14k; option 4 = T-06 (20k) + reserve 4k; option 5 = T-07+T-08+T-09 (51k) + reserve 10k — each plus a proportional T-13 verification slice if run standalone.
- Checkpoint math applies at every checkpoint: under 60% continue; 60–150% note in ledger; over 150% intervene; twice over 150% = mandatory rescope with DEC entry.

## Concurrency and Write-Ownership Plan

- **Wave 0 (solo): T-01.** Nothing else runs. Rationale: until T-01 lands, npm test beside the live backend is destructive (ISS-001), and every later task's verification wants a safe suite. WP-001's own sequencing note says FIRST.
- **Wave 1 (parallel groups, disjoint files):** G2 agent runs T-02 then T-03 (owns src/store/useStore.ts, src/components/Shell.tsx, src/screens/Onboarding.tsx, Lock.tsx, Dashboard.tsx). G3 agent runs T-04 then T-05 (owns src/screens/Assistant.tsx, src/lib/ai.ts, src/screens/Automations.tsx). Impl lead inline: T-06 (server/connectors.mjs) then G5 start T-07 (render.yaml, README, package.json engines, ci.yml node lines).
- **Wave 2 (lead inline): T-08** (server/index.mjs — after G2 lands so the Lock payload contract is settled), **T-09** (package.json — after T-07's edit to the same file), **T-12** (ci.yml — strictly after T-01; same-file serial with T-07's CI touch).
- **Wave 3 (runtime-exclusive): T-10 then T-11.** G7 owns the emulator, backend process, and HOMEOPS_DATA_DIR flip; no other task may touch :8787/:5173 or run the suite concurrently. Environment restored (default .data, health 200) before the group closes.
- **Final (solo): T-13.**
- **File-ownership rule**: no file has two writers in the same wave; package.json and ci.yml are single-writer (impl lead) across all waves. The emulator + backend runtime is a serialized resource mission-wide — in-slice emulator checks in waves 1–2 run only inside the owning agent's turn.
- **Register ownership restated**: canonical registers under audit/ stay single-writer (orchestrator or named synthesis lead). Implementers write agent-deltas/ and implementation/ only. Journal and artifact ledger are append-only via mem scripts. phase-state.md belongs to set_phase.py. handoffs/ are never edited after dispatch.

## Authority Constraints

No route in this guide may, without new authorization from the user via the orchestrator:

- mutate git (commit, push, branch, PR), deploy anywhere, or probe production beyond at most ONE user-authorized read-only GET (HYP-006);
- read .env, D:\HomeOps\SubscriptionKey_3A6LNPV3XG.p8, D:\HomeOps\twilio_2FA_recovery_code.txt, or vault contents under server/.data;
- execute connector actions with external side effects (messages, SMS, payments, calendar writes), configure OAuth providers, or trigger ANY auth flow — including claude.ai connector OAuth and computer-use request_access — to test or fill a capability claim;
- install new dependencies, browsers, or Node versions (downloads are permission-gated; T-07's local boot-smoke limitation follows from this);
- edit phase-state.md, handoffs/, or canonical audit/ registers; delete or rewrite journal/ledger rows; alter snapshots;
- present unverifiable success: no claimed pass without runnable output or inspected evidence — economy never buys out evidence (budget-policy drift list).

## Unavailable Capability Effects

| Capability | State | Effect on plan | Workaround | Unblock condition |
|---|---|---|---|---|
| Claude_Browser in-app pane (full drive) | degraded/unverified — pane + seed tab observed this session, but audit recorded screenshot hangs + origin-approval denials non-interactive | None material — emulator work unaffected | Playwright MCP primary for all product interaction (audit-proven, schema-verified) | An interactive user approves origins in-session; revalidate before first use |
| claude.ai connector MCPs (Gmail, Calendar, Notion, Drive, Figma, +33 pending auth) | blocked (OAuth required; non-interactive session cannot authenticate) | None — no selected task requires an external connector | Not needed; never force a plugin to fill a row | User authorizes via claude.ai connector settings or /mcp in an interactive session — never agent-triggered |
| computer-use MCP | blocked (interactive request_access approval required) | None — no desktop-app task exists | Playwright covers all browser needs | User grants access in an interactive session |
| claude-in-chrome MCP | blocked/unverified (requires user's Chrome + connected extension) | None | Playwright | User connects the extension |
| GitHub Actions execution | blocked (runs on push; git mutations barred) | T-12 verified only by YAML validity + local step simulation; end-to-end CI proof deferred | Scripted local reproduction of the exact job steps with hash assertion | User pushes (or authorizes push) after selection delivery |
| Render production | blocked (no deploy authority; no confirmed URL — HYP-006 open) | T-07 deploy-safety proven as config-consistency only; prod/source parity stays unresolved and labeled | Consistency table + CI-as-proxy once pushed | User supplies the live URL for a single read-only GET, or authorizes a deploy dry-run |
| Node inside the reconciled engines range | unavailable locally (nvm has 20.20.2 / 22.12.0 / 25.8.2 — none satisfies '>=22.13 <25') | T-07 cannot boot-smoke the pinned version on this machine today | Config-consistency check now; boot smoke via CI node once pushed | User approves installing a matching Node (e.g. 22.13+ or 24.x via nvm) |
| npm test as a safe verification tool | blocked until T-01 lands (ISS-001: suite corrupts live .data beside a running backend) | Forces wave-0 sequencing; pre-T-01 suite runs require the hash-guard protocol and a planned backend restart | T-01 first; interim runs only with pre/post hash + restart plan | T-01 merged and proven |

## Reassessment Triggers

Standard set (matching-policy): a capability appears, disappears, or changes auth state; the host's model/effort choices change; any task budget overruns 150% at a checkpoint; new tasks enter scope or the selection changes; a route fails twice for the same cause; phase transition into implementation — mandatory revalidation of drift-prone facts (servers healthy on :8787/:5173/:9223, Playwright MCP loadable in the implementing session, model-alias enum, .data claimed-state, node version, dirty-file set still exactly .claude/launch.json, 287-test baseline).

Mission-specific:

- The user's selection at the gate differs from the full slate → prune the Task Matrix and recompute totals from the scenario table (per-option math provided in the ledger).
- HYP-001 resolution contradicts the claimed-server root-cause theory → re-scope T-03 before further G2 work and record a DEC entry.
- The Vite cold-start crash (exit 0xC0000005 during dependency re-optimization, observed once at audit bootstrap) recurs during emulator work → add a stabilization task; do not silently retry past two occurrences.
- ISS-001 signature reappears at any point (disk I/O 500s on the live backend) → stop all work, restart backend via the known-good command, append a correction event before resuming.
- Claude_Browser pane becomes interactive (user present, origins approvable) → optional secondary emulator; re-run its inventory row first.
- Any Node install is authorized → upgrade T-07's verification from consistency-check to local boot smoke.
- Implementation-lead session exposes a different model enum or agent-type list than this session → rebuild the Model column against their observed enum before first dispatch.
