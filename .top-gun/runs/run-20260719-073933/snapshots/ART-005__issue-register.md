# Master Issue Register

Populated by audit-lead, run-20260719-073933. One deduplicated row per root cause. Root-cause labels: [observed] proven by evidence; [inferred] labeled inference.

| ISS-ID | Title | Severity | Confidence | Personas | FEAT/JRN IDs | Visual symptom | Technical symptom | Root cause | Evidence IDs | Impact | Recommended resolution | Files/services | Dependencies | Acceptance criteria | Validation | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| ISS-001 | Test suite writes to the LIVE data dir and bricks a running backend (`npm test` → permanent "disk I/O error" on every API route until restart) | P0 | observed | all (any dev/CI runbook) | FEAT-166, JRN-1 | None until later: app silently degrades | After `npm test`, every backend route incl. /api/health returns 500 `{"error":"server_error","message":"disk I/O error"}`; live audit.jsonl gained `run.stalled runId=run_stalltest` at 2026-07-19T07:55:52Z; live household.db mtime = test window | [observed] `server/test/engine-hardening.test.mjs:15-16` imports `../store.mjs` + `../engine.mjs` directly into the test-runner process where HOMEOPS_DATA_DIR is unset → store resolves the REAL `server/.data`; test then createRun()s a fake run and runs `engine.expireStaleRuns()` against the live tenant DB while the dev server holds an open node:sqlite handle → server handle goes stale → SQLITE_IOERR on all subsequent requests. Harness isolation (mkdtemp) is bypassed because these are in-process imports, not the spawned server | EV-116, EV-117, EV-118, EV-104 | Data integrity: test data written into real household DB + live audit log; developer backend bricked silently; the sweeper mutates real runs. On a machine with real family data this contaminates and can corrupt it | Make in-process imports impossible to hit the real store: harness must set HOMEOPS_DATA_DIR in the TEST process env (not only the child), and `store.mjs` should refuse to open the default dir when NODE_TEST_CONTEXT is set; rewrite the stall test to seed via the spawned server's API or a store handle bound to ctx.dataDir | server/test/engine-hardening.test.mjs, server/test/harness.mjs, server/store.mjs | none | `npm test` with dev server running leaves live .data byte-identical and server healthy; stall test still proves sweeper behavior | Re-run suite with server up; hash .data before/after; health stays 200 | open |
| ISS-003 | Locally-created household is orphaned and hidden whenever the server's resident tenant is already claimed (or claim fails): Lock screen shows the server roster, the new Owner's profile vanishes, and onboarding never routes to the email-signup path that would actually work | P1 | observed | P1 Maya | FEAT-001, FEAT-004, FEAT-005, FEAT-006, JRN-1 | After onboarding "Create household", reload shows "Who's using FamiliOS?" with a stranger's profile (dev roster "Ross"); Maya's profile gone; household-name subtitle gone leaving lowercase fragment "choose a profile to continue." | GET /api/profiles (unauthenticated, origin-gated) hardcodes householdId:"local" (server/index.mjs:573-585); Lock.tsx:75-82 treats server registry as truth and hides local members; claim is one-time (409 already_claimed once any non-seed member exists, index.mjs:597-601); IndexedDB still holds the full local household (verified via idb inspection) | [observed] resident-household model (one claimable "local" tenant per server) + server-first lock roster + onboarding that never checks `claimed` before offering the doomed local-claim path; self-heal claim in useStore.ts:615-627 is unreachable because the UI hides the local owner profile | EV-123, EV-124, EV-125 | Fresh users on any already-claimed server (incl. every second household on a shared deployment, or after server data reset mismatch) lose access to a household they just created; data silently stranded in IndexedDB; violates local-first trust promise | Onboarding should read /api/profiles `claimed` flag and, when claimed, route Create-household to email signup (stranger tenant) with honest copy; Lock should merge/label local-only households ("On this device — not registered with this server") instead of hiding them; fix subtitle fragment | src/screens/Onboarding.tsx, src/screens/Lock.tsx, src/store/useStore.ts, server/index.mjs /api/profiles | ISS-002 | Fresh onboarding on a claimed server lands the user in a working household (server-backed via signup) or clearly labels the local-only household; local data never silently hidden | Fresh-profile emulator run on claimed server; assert no orphan state | open |
| ISS-003b | Same root cause, second symptom: Settings → "Reset sample data" (and the onboarding "Explore the sample" card) dead-ends at a Lock screen with a stranger's profile on any claimed server — the Harper sample loads locally but no sample member can authenticate | P1 | observed | P1 Maya, all evaluators | FEAT-003, FEAT-146, JRN-1 | Click "Reset to The Harper Family" → 403 /api/session → Lock "Who's using FamiliOS?" shows only "Ross/Owner"; the Harper family is loaded in IndexedDB but unreachable | [observed] reseed()/completeOnboarding("sample") call loginAs(m-alex) which the claimed server rejects (demo roster archived at claim); Lock then shows server roster (Ross), not the local Harper members | EV-138, EV-125 | The product's primary "try it risk-free" affordance and its sample-reset recovery are broken on every real (claimed) deployment; a reviewer/first-user sees someone else's name and cannot proceed | Treat sample/local households as client-only sessions that don't require a server actor (mint a local session for sample mode), OR clearly separate "sample mode" from server-backed mode in the UI | src/store/useStore.ts (reseed, completeOnboarding, loginAs), src/screens/Lock.tsx | ISS-003 | "Reset sample data" on a claimed server lands in the working Harper sample (local session), never a foreign Lock | Reseed on claimed server, assert Harper dashboard | open |
| ISS-002 | Fresh-install onboarding swallows total backend failure: no error surfaced at claim, misleading "may need PIN", dashboard renders as if healthy with all fetches failing | P1 | observed | P1 Maya (fresh Owner) | FEAT-001, FEAT-004, FEAT-005, FEAT-016, JRN-1 | Create household → silently dropped at "Who's using FamiliOS?" lock; after tapping profile, normal-looking dashboard; "Connected systems: No connectors loaded" empty state | POST /api/household/claim 500, GET /api/profiles 500 x2, GET /api/session 500, /api/health 500, then 20+ 401s on every data fetch; zero user-visible error, no offline banner; mobile shell has no runtime pill (it lives only in sidebar/drawer) | [observed] onboarding/lock/store flows catch backend errors and fall through to local-first defaults without surfacing degraded state; RuntimePill exists only in Sidebar (lg+) and drawer, not in mobile shell or dashboard; empty-state copy indistinguishable from failure-state | EV-119, EV-120, EV-121 | Trust: violates the product's own "no fake success states" promise (PI-005) at the single most critical moment (first run); user cannot know connectors/approvals/AI are dead; "may need PIN" implies user error instead of server fault | Surface a degraded-mode banner (StorageBanner-pattern) whenever session/claim/health fail; distinguish "empty because new" from "empty because unreachable" in dashboard tiles; fix "may need PIN" copy to reflect actual state; consider a mobile runtime status affordance | src/screens/Onboarding.tsx, src/screens/Lock.tsx, src/store/useStore.ts, src/components/Shell.tsx (RuntimePill), src/screens/Dashboard.tsx | none | With backend 500ing, fresh onboarding shows an explicit reachable/unreachable state at each failing step; dashboard shows degraded banner; no silent 401 loops | Kill backend (dev), run fresh onboarding, verify banners + copy | open |
| ISS-004 | "Ask FamiliOS" chat (the headline feature) dead-ends without an AI provider, while the equivalent Workflow Builder offers a working "built-in rules engine" fallback — inconsistent, and it contradicts the local-first "every task resolves locally" positioning | P1 | observed | P1 Maya, P4 Elaine, all | FEAT-020, FEAT-021, FEAT-042, JRN-1 | Assistant: tapping the seeded suggestion chip "Plan three dinners for this week and start a grocery list" returns "No AI provider is connected. Add one in Settings → AI Providers, then ask me again." Workflow Builder: same class of request, "or use the built-in rules engine" produces a real, enabled automation with zero provider | [observed] the deterministic engine (`src/lib/ai.ts`, "every task resolves locally") powers NL→agent and NL→workflow build, but the Assistant chat surface routes free-form input only to the provider path and offers no local fallback; suggestion chips promise capability the chat won't deliver offline | EV-128, EV-130, EV-131, EV-107 | The single most prominent surface (top bar "Ask FamiliOS", bottom-nav Ask, dashboard hero well) is a dead-end out of the box, undercutting the product's headline and its local-first promise; users on the free/no-provider path (the documented default) hit a wall at the first thing they try | Give the Assistant chat the same deterministic fallback the Workflow Builder has: answer capability questions and produce approvable plans locally, and reserve the "add a provider" prompt for genuinely provider-only tasks; align suggestion chips with what the local engine can do | src/screens/Assistant.tsx, src/lib/ai.ts, src/screens/AIProviders.tsx | none | With no provider, a planning-type chat request yields a local plan or an honest, scoped "needs a provider for X" — never a blanket dead-end; capability question answered locally | Emulator: no-provider chat of a planning prompt returns a local plan | open |
| ISS-006 | Flagship "live" Weather tool returns a hollow success (no temperature/conditions) instead of an honest provider_error — the one thing the product promises never to do | P2 | observed | P1 Maya, all | FEAT-062, JRN-1 | Weather connector detail → tool "Current conditions" → Run → result panel shows `{ "location": "Home", "fetchedAt": "2026-07-19T08:30:04.185Z" }` with NO temperature, wind, or weather code, yet rendered as a successful run | POST /api/tools/weather.current/execute → 200; backend executor (server/connectors.mjs:397-402) does `const r = await fetch(...); const j = await r.json();` with NO `r.ok` check and NO try/catch-to-provider_error, unlike the sibling RSS handler (:406) and HTTP handler which return `{ok:false, error:"provider_error"}`; when `j.current` is absent, `temperatureC/windKph/weatherCode` are `undefined` and JSON.stringify drops them → hollow object presented as success. Host egress to Open-Meteo confirmed working (EV-135), so this is a code-robustness defect, not a network outage | [observed] missing readiness/error-mapping in the weather executor; the flagship out-of-box connector is the one with the least-defensive executor | EV-134, EV-135, EV-136 | Directly contradicts PI-005 ("no fake success states") on the single connector every new user meets first; briefings built on this get silent empty weather | Mirror the RSS/HTTP pattern: check `r.ok`, validate `j.current` exists, return `{ok:false,error:"provider_error",message}` otherwise; surface the failure in the tool result panel | server/connectors.mjs (weather.current executor ~L397-402) | none | Weather tool with an unreachable/invalid upstream returns a visible provider_error, never a hollow success; valid upstream shows temperature | Force a bad lat/lon or upstream error; assert provider_error surfaced | open |
| ISS-005 | Rules-engine workflow builder ignores schedule phrasing: "Every morning at 7am …" produces an automation with trigger "Manual" instead of "Schedule" | P2 | observed | P1 Maya | FEAT-042, JRN-1 | New automation card reads trigger "Manual" though the description explicitly says "Every morning at 7am" | [inferred] the built-in rules path that instantiates the automation does not apply `detectTrigger()` (src/lib/ai.ts:193-207 correctly maps "every morning"/"7am"→"Schedule"), or the builder hardcodes Manual for rules-engine output; provider path may differ | EV-131, EV-132 | The automation silently won't fire on schedule; a user who "built" a morning routine gets one that never runs unless manually triggered — a quiet broken-promise | Route rules-engine automation creation through `detectTrigger()`; show the resolved trigger in the plan preview before activation | src/screens/Automations.tsx, src/lib/ai.ts | ISS-004 | Building "every morning at 7am …" via the rules engine yields a Schedule trigger; preview shows it | Rebuild via rules engine, assert trigger=Schedule | open |
| ISS-007 | Incoherent Node version story creates a real production deploy risk: CI runs Node 24, package.json engines demands ">=22.13 <25", local dev runs 25.8.2 (violates engines), and render.yaml pins NO Node version (uses Render's default) | P2 | observed | operator/deploy | FEAT-166, FEAT-167 | None in-app; a deploy-time/runtime failure surface | [observed] `.github/workflows/ci.yml:20,36` node 24; `apps/mobile` job node 20; `package.json` engines ">=22.13 <25"; README "Node 18+ (built on Node 20)"; `render.yaml` has no NODE_VERSION/engines pin; tenant store needs node:sqlite (flag-free ≥22.13) so a Render default <22.13 or a mismatched major breaks the DB layer at boot | EV-106, EV-143 | Production could boot on a Node the code was never tested against; node:sqlite/tenant-db is the blast radius; CI green gives false confidence because CI's Node ≠ Render's Node | Pin Node everywhere: add `NODE_VERSION` to render.yaml (or `.nvmrc`/`engines` honored by Render), reconcile engines to include the intended runtime, fix README; make CI Node match production Node | render.yaml, package.json (engines), README.md, .github/workflows/ci.yml | none | render.yaml pins a Node in the engines range; CI uses the same; boot smoke test passes on that version | Add NODE_VERSION, redeploy path dry-run | open |
| ISS-008 | ISS-001 is invisible to CI: `npm test` on GitHub runners has no concurrent live server and a fresh empty `.data`, so the leaky in-process test never collides — the P0 only bites a developer/operator running tests beside a live backend | P2 | observed | operator/dev | FEAT-166 | CI is green while the P0 exists | [observed] ci.yml runs `npm test` in isolation; the leak (ISS-001) needs a running server holding a node:sqlite handle on the same default `.data`, which CI never has | EV-143, EV-116 | Explains why a data-integrity P0 shipped; the test gate cannot see it, so it will recur until the isolation is fixed structurally (ISS-001) | Fix via ISS-001 (env-scope the store in-process); optionally add a CI step that runs the server then the suite to reproduce | server/test/harness.mjs, server/store.mjs, .github/workflows/ci.yml | ISS-001 | After the ISS-001 fix, a CI job that starts the server then runs tests leaves .data intact | Add reproduction job | open |
| ISS-009 | Unauthenticated household-roster exposure: GET /api/profiles returns members' display names, roles, and relationships for the resident "local" tenant before any authentication (origin-gated only) | P2 | observed | privacy/security | FEAT-005, FEAT-160 | Lock screen shows the roster to anyone who can load the page | [observed] server/index.mjs:573-585 returns names/roles/relationship pre-auth for householdId:"local"; guarded only by `isAllowedOrigin`. Justified as "a family device must show who can sign in", but on a public deployment it discloses the household's member list, roles, and relationships (e.g., "Child (age 9)", "Babysitter") to any same-origin unauthenticated request | EV-125 | Privacy: family composition incl. children's presence and a caregiver's role is readable without credentials; low exploit complexity, meaningful for a family product | Gate the roster behind a device-pairing token or a first-run bootstrap that stops exposing it once claimed; at minimum drop `relationship` and age hints from the pre-auth payload | server/index.mjs (/api/profiles) | none | Pre-auth /api/profiles on a claimed public deployment returns no PII beyond what device-pairing requires | Curl /api/profiles unauth on claimed tenant; assert minimized payload | open |
| ISS-010 | Code debt: `react-router-dom` ^6.28 is a production dependency but never imported anywhere in the web app (navigation is Zustand store-based); ships dead weight in the bundle and misleads contributors | P3 | observed | maintainer | FEAT-166 | None user-facing | [observed] grep of src/ shows zero imports of react-router-dom; App.tsx uses a `route.screen` store field and a SCREENS map | EV-102 | Bundle bloat + onboarding confusion (a new dev expects routes); minor | Remove the dependency, or adopt it intentionally for real URL routing (which would also fix deep-linking/refresh-to-screen) | package.json, src/ | none | Dependency removed or actually used; bundle no longer includes it | Build + bundle inspect | open |
