# Hypothesis Queue — run-20260723-143314

| HYP-ID | Question | Owner | Confidence | Needed evidence | Next discriminating check | Status | Resolution |
|---|---|---|---|---|---|---|---|
| HYP-101 | Is the created-event-invisible bug (ISS-105) a client refresh gap, a layer/space filter, or a silent write failure? | implementation lead | unresolved | API truth vs UI state at the same moment | Create an event in the app, then `GET /api/events` immediately. Present ⇒ client refresh/filter; absent ⇒ silent POST failure. **One check splits all three causes.** | open | — |
| HYP-102 | Does the delete path ever call Google's delete for `linked` events (ISS-106)? | implementation lead | unresolved | Delete route trace + Google API call log | Read the delete handler for a `provenance.googleEventId` branch; if absent, cause is proven without runtime | open | — |
| HYP-103 | Which surface produced the "you still have groceries" count that the Shared Grocery List contradicts (ISS-112)? | implementation lead | partially resolved | The two selectors side by side | `BOARD_TASK_TYPES` (EV-112) proves types are partitioned; still need the specific count query the dashboard/meals surface uses | open | Mechanism confirmed; exact producing surface not yet named |
| HYP-104 | How many resident/production skills actually lack `defaultAgentId` (ISS-102 blast radius)? | implementation lead | unresolved | Production tenant skill export | Count skills where `defaultAgentId` is null via the server export API | open | — |
| HYP-105 | Did Part 1's incomplete intent extraction (0:00–1:46 of 20:01) hide additional distinct defects? | orchestrator | unresolved | Full coverage of EV-102 | Re-split that recording into <10-min segments and re-run extraction, or review manually | open | Its long embedded voiceover was mined, but per-moment structured data is missing |
| HYP-106 | Does a `needs_reconnect` Google account still contribute rendered events, compounding ISS-104? | implementation lead | unresolved | Event provenance vs account status | Query events whose `provenance.subscriptionId` maps to the disconnected account and check whether they render | open | — |
| HYP-107 | Is there a general pattern of success toasts firing before async work completes (ISS-120, ISS-116)? | implementation lead | weakly-inferred | Toast emission points vs completion callbacks | Audit every `toast({kind:"success"})` call site for whether it awaits the underlying promise | open | — |
| HYP-108 | Will ISS-101's production cleanup immediately re-fill with duplicates without ISS-111's idempotency fix? | orchestrator | strongly-inferred | Production generation patterns | Sequence: fix idempotency first, then migrate — or migrate and monitor duplicate creation for one week | open | Drives the WP sequencing decision in DEC-104 |
